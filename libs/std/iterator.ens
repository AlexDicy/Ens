// The iteration contract used by `for (item in collection)`. Any class with a
// `makeIterator() -> Iterator<T>` method can be iterated; arrays are iterated
// directly without an iterator object.
public abstract class Iterator<T> {
    abstract hasNext() -> bool;
    abstract next() -> T;
}
