import Iterable from @std.collections.iterator;

// An iterable with a count and a membership test.
export interface Collection<T> extends Iterable<T> {
    length() -> long;
    isEmpty() -> bool;
    contains(T value) -> bool;
}
