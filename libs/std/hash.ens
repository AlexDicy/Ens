// The hashing contract used by keyed collections such as Map and Set.
// Every Ens type satisfies it automatically: the compiler synthesizes
// hash() for any type that does not define its own. A class can declare
// `hash() -> long` to control how its instances hash; by default classes
// hash by identity and value types hash by their contents.
public interface Hashable {
    hash() -> long;
}
