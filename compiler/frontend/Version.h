#pragma once

namespace ens {

// The toolchain version, aligned with the language version packages declare (`ens "0.1";`).
inline constexpr const char* kToolchainVersion = "0.1";

}  // namespace ens
