// Generated by syntaxgen from selfhost/syntax.grammar. Do not edit.
import GreenNode from syntax.green;
import NodeFactory from syntax.node;
import SyntaxNode from syntax.node;
import SyntaxToken from syntax.node;

public interface TopLevelItem { }

public interface Member { }

public interface Argument { }

public interface ArmBody { }

public interface ElseBody { }

public interface ForInitializer { }

public sealed abstract class Declaration extends SyntaxNode {
    Declaration(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

public sealed abstract class Statement extends SyntaxNode {
    Statement(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

public sealed abstract class Expression extends SyntaxNode implements Argument, ArmBody {
    Expression(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

public sealed abstract class Type extends SyntaxNode {
    Type(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

public sealed abstract class SwitchArm extends SyntaxNode {
    SwitchArm(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

// Container for list slots: its children are the list elements, with the separator tokens
// interleaved at the odd indices for separated lists.
public class ListNode extends SyntaxNode {
    ListNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    childCount() -> long {
        return this.slotCount();
    }

    childNodeAt(long index) -> SyntaxNode {
        return this.nodeAt(index);
    }

    childTokenAt(long index) -> SyntaxToken {
        return this.tokenAt(index);
    }
}

public class SourceFileNode extends SyntaxNode {
    SourceFileNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    itemsCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    items(long index) -> TopLevelItem {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is TopLevelItem) {
                return node;
            }
        }
        panic("Internal: expected TopLevelItem in the list at slot 0 of a {this.kind()} node");
    }

    eof() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

public class FunctionDeclarationNode extends Declaration implements TopLevelItem, Member {
    FunctionDeclarationNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    modifiersCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    modifiers(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    name() -> SyntaxToken {
        return this.tokenAt(1);
    }

    typeParameters() -> TypeParameterListNode? {
        let node = this.nodeAtOrNull(2);
        return node as? TypeParameterListNode;
    }

    parameters() -> ParameterListNode {
        let node = this.nodeAt(3);
        if (node is ParameterListNode) {
            return node;
        }
        panic("Internal: expected ParameterListNode in slot 3 of a {this.kind()} node");
    }

    returnType() -> ReturnTypeClauseNode? {
        let node = this.nodeAtOrNull(4);
        return node as? ReturnTypeClauseNode;
    }

    throwsClause() -> ThrowsClauseNode? {
        let node = this.nodeAtOrNull(5);
        return node as? ThrowsClauseNode;
    }

    body() -> BlockNode? {
        let node = this.nodeAtOrNull(6);
        return node as? BlockNode;
    }

    semicolon() -> SyntaxToken? {
        return this.tokenAtOrNull(7);
    }

    catchClausesCount() -> long {
        let list = this.nodeAt(8);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 8 of a {this.kind()} node");
    }

    catchClauses(long index) -> CatchClauseNode {
        let list = this.nodeAt(8);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is CatchClauseNode) {
                return node;
            }
        }
        panic("Internal: expected CatchClauseNode in the list at slot 8 of a {this.kind()} node");
    }
}

public class ParameterListNode extends SyntaxNode {
    ParameterListNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    openParenthesis() -> SyntaxToken {
        return this.tokenAt(0);
    }

    parametersCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    parameters(long index) -> ParameterNode {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is ParameterNode) {
                return node;
            }
        }
        panic("Internal: expected ParameterNode in the list at slot 1 of a {this.kind()} node");
    }

    closeParenthesis() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class ParameterNode extends SyntaxNode {
    ParameterNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    outKeyword() -> SyntaxToken? {
        return this.tokenAtOrNull(0);
    }

    declaredType() -> Type? {
        let node = this.nodeAtOrNull(1);
        return node as? Type;
    }

    thisPrefix() -> ThisPrefixNode? {
        let node = this.nodeAtOrNull(2);
        return node as? ThisPrefixNode;
    }

    name() -> SyntaxToken {
        return this.tokenAt(3);
    }

    defaultValue() -> InitializerNode? {
        let node = this.nodeAtOrNull(4);
        return node as? InitializerNode;
    }
}

public class ThisPrefixNode extends SyntaxNode {
    ThisPrefixNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    thisKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    dot() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

public class InitializerNode extends SyntaxNode {
    InitializerNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    equals() -> SyntaxToken {
        return this.tokenAt(0);
    }

    value() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }
}

public class ReturnTypeClauseNode extends SyntaxNode {
    ReturnTypeClauseNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    arrow() -> SyntaxToken {
        return this.tokenAt(0);
    }

    returnedType() -> Type {
        let node = this.nodeAt(1);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 1 of a {this.kind()} node");
    }
}

public class ThrowsClauseNode extends SyntaxNode {
    ThrowsClauseNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    throwsKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    typesCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    types(long index) -> NamedTypeNode {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is NamedTypeNode) {
                return node;
            }
        }
        panic("Internal: expected NamedTypeNode in the list at slot 1 of a {this.kind()} node");
    }
}

public class CatchClauseNode extends SyntaxNode {
    CatchClauseNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    catchKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    caughtType() -> NamedTypeNode {
        let node = this.nodeAt(2);
        if (node is NamedTypeNode) {
            return node;
        }
        panic("Internal: expected NamedTypeNode in slot 2 of a {this.kind()} node");
    }

    name() -> SyntaxToken {
        return this.tokenAt(3);
    }

    closeParenthesis() -> SyntaxToken {
        return this.tokenAt(4);
    }

    body() -> BlockNode {
        let node = this.nodeAt(5);
        if (node is BlockNode) {
            return node;
        }
        panic("Internal: expected BlockNode in slot 5 of a {this.kind()} node");
    }
}

public class StructDeclarationNode extends Declaration implements TopLevelItem {
    StructDeclarationNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    modifiersCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    modifiers(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    structKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    typeParameters() -> TypeParameterListNode? {
        let node = this.nodeAtOrNull(3);
        return node as? TypeParameterListNode;
    }

    extendsClause() -> ExtendsClauseNode? {
        let node = this.nodeAtOrNull(4);
        return node as? ExtendsClauseNode;
    }

    implementsClause() -> ImplementsClauseNode? {
        let node = this.nodeAtOrNull(5);
        return node as? ImplementsClauseNode;
    }

    body() -> MemberListNode {
        let node = this.nodeAt(6);
        if (node is MemberListNode) {
            return node;
        }
        panic("Internal: expected MemberListNode in slot 6 of a {this.kind()} node");
    }
}

public class ClassDeclarationNode extends Declaration implements TopLevelItem {
    ClassDeclarationNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    modifiersCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    modifiers(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    classKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    typeParameters() -> TypeParameterListNode? {
        let node = this.nodeAtOrNull(3);
        return node as? TypeParameterListNode;
    }

    extendsClause() -> ExtendsClauseNode? {
        let node = this.nodeAtOrNull(4);
        return node as? ExtendsClauseNode;
    }

    implementsClause() -> ImplementsClauseNode? {
        let node = this.nodeAtOrNull(5);
        return node as? ImplementsClauseNode;
    }

    body() -> MemberListNode {
        let node = this.nodeAt(6);
        if (node is MemberListNode) {
            return node;
        }
        panic("Internal: expected MemberListNode in slot 6 of a {this.kind()} node");
    }
}

public class InterfaceDeclarationNode extends Declaration implements TopLevelItem {
    InterfaceDeclarationNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    modifiersCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    modifiers(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    interfaceKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    typeParameters() -> TypeParameterListNode? {
        let node = this.nodeAtOrNull(3);
        return node as? TypeParameterListNode;
    }

    body() -> MemberListNode {
        let node = this.nodeAt(4);
        if (node is MemberListNode) {
            return node;
        }
        panic("Internal: expected MemberListNode in slot 4 of a {this.kind()} node");
    }
}

public class ExtendsClauseNode extends SyntaxNode {
    ExtendsClauseNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    extendsKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    base() -> NamedTypeNode {
        let node = this.nodeAt(1);
        if (node is NamedTypeNode) {
            return node;
        }
        panic("Internal: expected NamedTypeNode in slot 1 of a {this.kind()} node");
    }
}

public class ImplementsClauseNode extends SyntaxNode {
    ImplementsClauseNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    implementsKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    interfacesCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    interfaces(long index) -> NamedTypeNode {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is NamedTypeNode) {
                return node;
            }
        }
        panic("Internal: expected NamedTypeNode in the list at slot 1 of a {this.kind()} node");
    }
}

public class MemberListNode extends SyntaxNode {
    MemberListNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    openBrace() -> SyntaxToken {
        return this.tokenAt(0);
    }

    membersCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    members(long index) -> Member {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is Member) {
                return node;
            }
        }
        panic("Internal: expected Member in the list at slot 1 of a {this.kind()} node");
    }

    closeBrace() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class FieldDeclarationNode extends Declaration implements Member {
    FieldDeclarationNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    modifiersCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    modifiers(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    declaredType() -> Type {
        let node = this.nodeAt(1);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 1 of a {this.kind()} node");
    }

    name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    defaultValue() -> InitializerNode? {
        let node = this.nodeAtOrNull(3);
        return node as? InitializerNode;
    }

    semicolon() -> SyntaxToken {
        return this.tokenAt(4);
    }
}

public class EnumDeclarationNode extends Declaration implements TopLevelItem {
    EnumDeclarationNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    modifiersCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    modifiers(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    enumKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    openBrace() -> SyntaxToken {
        return this.tokenAt(3);
    }

    membersCount() -> long {
        let list = this.nodeAt(4);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 4 of a {this.kind()} node");
    }

    members(long index) -> EnumMemberNode {
        let list = this.nodeAt(4);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is EnumMemberNode) {
                return node;
            }
        }
        panic("Internal: expected EnumMemberNode in the list at slot 4 of a {this.kind()} node");
    }

    closeBrace() -> SyntaxToken {
        return this.tokenAt(5);
    }
}

public class EnumMemberNode extends Declaration {
    EnumMemberNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    name() -> SyntaxToken {
        return this.tokenAt(0);
    }
}

public class ImportDeclarationNode extends Declaration implements TopLevelItem {
    ImportDeclarationNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    importKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    binding() -> ImportBindingNode? {
        let node = this.nodeAtOrNull(1);
        return node as? ImportBindingNode;
    }

    path() -> ImportPathNode {
        let node = this.nodeAt(2);
        if (node is ImportPathNode) {
            return node;
        }
        panic("Internal: expected ImportPathNode in slot 2 of a {this.kind()} node");
    }

    semicolon() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

public class ImportBindingNode extends SyntaxNode {
    ImportBindingNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    name() -> SyntaxToken {
        return this.tokenAt(0);
    }

    fromKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

public class ImportPathNode extends SyntaxNode {
    ImportPathNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    at() -> SyntaxToken? {
        return this.tokenAtOrNull(0);
    }

    segmentsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    segments(long index) -> SyntaxToken {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return list.childTokenAt(index * 2);
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }
}

public class ExternalTypeDeclarationNode extends Declaration implements TopLevelItem {
    ExternalTypeDeclarationNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    externalKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    typeKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    semicolon() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

public class ExternalBlockNode extends Declaration implements TopLevelItem {
    ExternalBlockNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    externalKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    fromKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    library() -> SyntaxToken {
        return this.tokenAt(2);
    }

    openBrace() -> SyntaxToken {
        return this.tokenAt(3);
    }

    functionsCount() -> long {
        let list = this.nodeAt(4);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 4 of a {this.kind()} node");
    }

    functions(long index) -> ExternalFunctionDeclarationNode {
        let list = this.nodeAt(4);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is ExternalFunctionDeclarationNode) {
                return node;
            }
        }
        panic("Internal: expected ExternalFunctionDeclarationNode in the list at slot 4 of a {this.kind()} node");
    }

    closeBrace() -> SyntaxToken {
        return this.tokenAt(5);
    }
}

public class ExternalFunctionDeclarationNode extends Declaration {
    ExternalFunctionDeclarationNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    name() -> SyntaxToken {
        return this.tokenAt(0);
    }

    parameters() -> ParameterListNode {
        let node = this.nodeAt(1);
        if (node is ParameterListNode) {
            return node;
        }
        panic("Internal: expected ParameterListNode in slot 1 of a {this.kind()} node");
    }

    returnType() -> ReturnTypeClauseNode? {
        let node = this.nodeAtOrNull(2);
        return node as? ReturnTypeClauseNode;
    }

    semicolon() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

public class TestDeclarationNode extends Declaration implements TopLevelItem {
    TestDeclarationNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    testKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    // This slot holds either a token or a node; exactly one accessor is non-null.
    descriptionToken() -> SyntaxToken? {
        return this.tokenAtOrNull(1);
    }

    descriptionNode() -> InterpolatedStringExpressionNode? {
        let node = this.nodeAtOrNull(1);
        return node as? InterpolatedStringExpressionNode;
    }

    body() -> BlockNode {
        let node = this.nodeAt(2);
        if (node is BlockNode) {
            return node;
        }
        panic("Internal: expected BlockNode in slot 2 of a {this.kind()} node");
    }
}

public class TypeParameterListNode extends SyntaxNode {
    TypeParameterListNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    less() -> SyntaxToken {
        return this.tokenAt(0);
    }

    parametersCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    parameters(long index) -> TypeParameterNode {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is TypeParameterNode) {
                return node;
            }
        }
        panic("Internal: expected TypeParameterNode in the list at slot 1 of a {this.kind()} node");
    }

    greater() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class TypeParameterNode extends SyntaxNode {
    TypeParameterNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    name() -> SyntaxToken {
        return this.tokenAt(0);
    }

    bounds() -> BoundsClauseNode? {
        let node = this.nodeAtOrNull(1);
        return node as? BoundsClauseNode;
    }
}

public class BoundsClauseNode extends SyntaxNode {
    BoundsClauseNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    colon() -> SyntaxToken {
        return this.tokenAt(0);
    }

    boundsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    bounds(long index) -> NamedTypeNode {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is NamedTypeNode) {
                return node;
            }
        }
        panic("Internal: expected NamedTypeNode in the list at slot 1 of a {this.kind()} node");
    }
}

public class NamedTypeNode extends Type {
    NamedTypeNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    qualifier() -> TypeQualifierNode? {
        let node = this.nodeAtOrNull(0);
        return node as? TypeQualifierNode;
    }

    name() -> SyntaxToken {
        return this.tokenAt(1);
    }

    typeArguments() -> TypeArgumentListNode? {
        let node = this.nodeAtOrNull(2);
        return node as? TypeArgumentListNode;
    }
}

public class TypeQualifierNode extends SyntaxNode {
    TypeQualifierNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    module() -> SyntaxToken {
        return this.tokenAt(0);
    }

    dot() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

public class ArrayTypeNode extends Type {
    ArrayTypeNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    element() -> Type {
        let node = this.nodeAt(0);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 0 of a {this.kind()} node");
    }

    openBracket() -> SyntaxToken {
        return this.tokenAt(1);
    }

    closeBracket() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class OptionalTypeNode extends Type {
    OptionalTypeNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    inner() -> Type {
        let node = this.nodeAt(0);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 0 of a {this.kind()} node");
    }

    question() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

public class TypeArgumentListNode extends SyntaxNode {
    TypeArgumentListNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    less() -> SyntaxToken {
        return this.tokenAt(0);
    }

    argumentsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    arguments(long index) -> Type {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is Type) {
                return node;
            }
        }
        panic("Internal: expected Type in the list at slot 1 of a {this.kind()} node");
    }

    greater() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class BlockNode extends Statement implements ArmBody, ElseBody {
    BlockNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    openBrace() -> SyntaxToken {
        return this.tokenAt(0);
    }

    statementsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    statements(long index) -> Statement {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is Statement) {
                return node;
            }
        }
        panic("Internal: expected Statement in the list at slot 1 of a {this.kind()} node");
    }

    closeBrace() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class VariableDeclarationNode extends Statement implements ForInitializer {
    VariableDeclarationNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    modifier() -> SyntaxToken? {
        return this.tokenAtOrNull(0);
    }

    declaredType() -> Type? {
        let node = this.nodeAtOrNull(1);
        return node as? Type;
    }

    name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    initializer() -> InitializerNode? {
        let node = this.nodeAtOrNull(3);
        return node as? InitializerNode;
    }

    semicolon() -> SyntaxToken {
        return this.tokenAt(4);
    }
}

public class IfStatementNode extends Statement implements ElseBody {
    IfStatementNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    ifKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    condition() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }

    closeParenthesis() -> SyntaxToken {
        return this.tokenAt(3);
    }

    thenBranch() -> BlockNode {
        let node = this.nodeAt(4);
        if (node is BlockNode) {
            return node;
        }
        panic("Internal: expected BlockNode in slot 4 of a {this.kind()} node");
    }

    elseClause() -> ElseClauseNode? {
        let node = this.nodeAtOrNull(5);
        return node as? ElseClauseNode;
    }
}

public class ElseClauseNode extends SyntaxNode {
    ElseClauseNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    elseKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    body() -> ElseBody {
        let node = this.nodeAt(1);
        if (node is ElseBody) {
            return node;
        }
        panic("Internal: expected ElseBody in slot 1 of a {this.kind()} node");
    }
}

public class WhileStatementNode extends Statement {
    WhileStatementNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    whileKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    condition() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }

    closeParenthesis() -> SyntaxToken {
        return this.tokenAt(3);
    }

    body() -> BlockNode {
        let node = this.nodeAt(4);
        if (node is BlockNode) {
            return node;
        }
        panic("Internal: expected BlockNode in slot 4 of a {this.kind()} node");
    }
}

public class ForStatementNode extends Statement {
    ForStatementNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    forKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    initializer() -> ForInitializer? {
        let node = this.nodeAtOrNull(2);
        return node as? ForInitializer;
    }

    initializerSemicolon() -> SyntaxToken? {
        return this.tokenAtOrNull(3);
    }

    condition() -> Expression? {
        let node = this.nodeAtOrNull(4);
        return node as? Expression;
    }

    semicolon() -> SyntaxToken {
        return this.tokenAt(5);
    }

    update() -> Expression? {
        let node = this.nodeAtOrNull(6);
        return node as? Expression;
    }

    closeParenthesis() -> SyntaxToken {
        return this.tokenAt(7);
    }

    body() -> BlockNode {
        let node = this.nodeAt(8);
        if (node is BlockNode) {
            return node;
        }
        panic("Internal: expected BlockNode in slot 8 of a {this.kind()} node");
    }
}

public class ForEachStatementNode extends Statement {
    ForEachStatementNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    forKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    letKeyword() -> SyntaxToken? {
        return this.tokenAtOrNull(2);
    }

    declaredType() -> Type? {
        let node = this.nodeAtOrNull(3);
        return node as? Type;
    }

    name() -> SyntaxToken {
        return this.tokenAt(4);
    }

    inKeyword() -> SyntaxToken {
        return this.tokenAt(5);
    }

    iterable() -> Expression {
        let node = this.nodeAt(6);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 6 of a {this.kind()} node");
    }

    closeParenthesis() -> SyntaxToken {
        return this.tokenAt(7);
    }

    body() -> BlockNode {
        let node = this.nodeAt(8);
        if (node is BlockNode) {
            return node;
        }
        panic("Internal: expected BlockNode in slot 8 of a {this.kind()} node");
    }
}

public class BreakStatementNode extends Statement {
    BreakStatementNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    breakKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    semicolon() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

public class ContinueStatementNode extends Statement {
    ContinueStatementNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    continueKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    semicolon() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

public class ReturnStatementNode extends Statement {
    ReturnStatementNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    returnKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    value() -> Expression? {
        let node = this.nodeAtOrNull(1);
        return node as? Expression;
    }

    semicolon() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class ExpressionStatementNode extends Statement implements ForInitializer {
    ExpressionStatementNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    expression() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    semicolon() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

public class ThrowStatementNode extends Statement {
    ThrowStatementNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    throwKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    value() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }

    semicolon() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class RethrowStatementNode extends Statement {
    RethrowStatementNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    rethrowKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    semicolon() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

public class SwitchStatementNode extends Statement {
    SwitchStatementNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    switchKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    scrutinee() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }

    closeParenthesis() -> SyntaxToken {
        return this.tokenAt(3);
    }

    openBrace() -> SyntaxToken {
        return this.tokenAt(4);
    }

    armsCount() -> long {
        let list = this.nodeAt(5);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 5 of a {this.kind()} node");
    }

    arms(long index) -> SwitchArm {
        let list = this.nodeAt(5);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is SwitchArm) {
                return node;
            }
        }
        panic("Internal: expected SwitchArm in the list at slot 5 of a {this.kind()} node");
    }

    closeBrace() -> SyntaxToken {
        return this.tokenAt(6);
    }
}

public class ValueArmNode extends SwitchArm {
    ValueArmNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    labelsCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    labels(long index) -> Expression {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is Expression) {
                return node;
            }
        }
        panic("Internal: expected Expression in the list at slot 0 of a {this.kind()} node");
    }

    arrow() -> SyntaxToken {
        return this.tokenAt(1);
    }

    body() -> ArmBody {
        let node = this.nodeAt(2);
        if (node is ArmBody) {
            return node;
        }
        panic("Internal: expected ArmBody in slot 2 of a {this.kind()} node");
    }
}

public class TypeArmNode extends SwitchArm {
    TypeArmNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    isKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    target() -> NamedTypeNode {
        let node = this.nodeAt(1);
        if (node is NamedTypeNode) {
            return node;
        }
        panic("Internal: expected NamedTypeNode in slot 1 of a {this.kind()} node");
    }

    binding() -> SyntaxToken? {
        return this.tokenAtOrNull(2);
    }

    arrow() -> SyntaxToken {
        return this.tokenAt(3);
    }

    body() -> ArmBody {
        let node = this.nodeAt(4);
        if (node is ArmBody) {
            return node;
        }
        panic("Internal: expected ArmBody in slot 4 of a {this.kind()} node");
    }
}

public class DefaultArmNode extends SwitchArm {
    DefaultArmNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    defaultKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    arrow() -> SyntaxToken {
        return this.tokenAt(1);
    }

    body() -> ArmBody {
        let node = this.nodeAt(2);
        if (node is ArmBody) {
            return node;
        }
        panic("Internal: expected ArmBody in slot 2 of a {this.kind()} node");
    }
}

public class LiteralExpressionNode extends Expression {
    LiteralExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    value() -> SyntaxToken {
        return this.tokenAt(0);
    }
}

public class IdentifierExpressionNode extends Expression {
    IdentifierExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    name() -> SyntaxToken {
        return this.tokenAt(0);
    }
}

public class ThisExpressionNode extends Expression {
    ThisExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    thisKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }
}

public class SuperExpressionNode extends Expression {
    SuperExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    superKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }
}

public class BinaryExpressionNode extends Expression {
    BinaryExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    left() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    operator() -> SyntaxToken {
        return this.tokenAt(1);
    }

    right() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }
}

public class PrefixExpressionNode extends Expression {
    PrefixExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    operator() -> SyntaxToken {
        return this.tokenAt(0);
    }

    operand() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }
}

public class PostfixExpressionNode extends Expression {
    PostfixExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    operand() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    operator() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

public class CallExpressionNode extends Expression {
    CallExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    callee() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    typeArguments() -> TypeArgumentListNode? {
        let node = this.nodeAtOrNull(1);
        return node as? TypeArgumentListNode;
    }

    arguments() -> ArgumentListNode {
        let node = this.nodeAt(2);
        if (node is ArgumentListNode) {
            return node;
        }
        panic("Internal: expected ArgumentListNode in slot 2 of a {this.kind()} node");
    }
}

public class ArgumentListNode extends SyntaxNode {
    ArgumentListNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    openParenthesis() -> SyntaxToken {
        return this.tokenAt(0);
    }

    argumentsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    arguments(long index) -> Argument {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is Argument) {
                return node;
            }
        }
        panic("Internal: expected Argument in the list at slot 1 of a {this.kind()} node");
    }

    closeParenthesis() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class OutArgumentNode extends SyntaxNode implements Argument {
    OutArgumentNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    outKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    name() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

public class NamedArgumentNode extends SyntaxNode implements Argument {
    NamedArgumentNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    name() -> SyntaxToken {
        return this.tokenAt(0);
    }

    colon() -> SyntaxToken {
        return this.tokenAt(1);
    }

    value() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }
}

public class MemberExpressionNode extends Expression {
    MemberExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    object() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    dot() -> SyntaxToken {
        return this.tokenAt(1);
    }

    member() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class SafeMemberExpressionNode extends Expression {
    SafeMemberExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    object() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    questionDot() -> SyntaxToken {
        return this.tokenAt(1);
    }

    member() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class SubscriptExpressionNode extends Expression {
    SubscriptExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    object() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    openBracket() -> SyntaxToken {
        return this.tokenAt(1);
    }

    index() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }

    closeBracket() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

public class SafeSubscriptExpressionNode extends Expression {
    SafeSubscriptExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    object() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    question() -> SyntaxToken {
        return this.tokenAt(1);
    }

    openBracket() -> SyntaxToken {
        return this.tokenAt(2);
    }

    index() -> Expression {
        let node = this.nodeAt(3);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 3 of a {this.kind()} node");
    }

    closeBracket() -> SyntaxToken {
        return this.tokenAt(4);
    }
}

public class CastExpressionNode extends Expression {
    CastExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    value() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    asKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    target() -> Type {
        let node = this.nodeAt(2);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 2 of a {this.kind()} node");
    }
}

public class CheckedCastExpressionNode extends Expression {
    CheckedCastExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    value() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    asKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    question() -> SyntaxToken {
        return this.tokenAt(2);
    }

    target() -> Type {
        let node = this.nodeAt(3);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 3 of a {this.kind()} node");
    }
}

public class TypeTestExpressionNode extends Expression {
    TypeTestExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    value() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    isKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    target() -> NamedTypeNode {
        let node = this.nodeAt(2);
        if (node is NamedTypeNode) {
            return node;
        }
        panic("Internal: expected NamedTypeNode in slot 2 of a {this.kind()} node");
    }
}

public class AssignmentExpressionNode extends Expression {
    AssignmentExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    target() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    operator() -> SyntaxToken {
        return this.tokenAt(1);
    }

    value() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }
}

public class TernaryExpressionNode extends Expression {
    TernaryExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    condition() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    question() -> SyntaxToken {
        return this.tokenAt(1);
    }

    thenValue() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }

    colon() -> SyntaxToken {
        return this.tokenAt(3);
    }

    elseValue() -> Expression {
        let node = this.nodeAt(4);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 4 of a {this.kind()} node");
    }
}

public class NullCoalesceExpressionNode extends Expression {
    NullCoalesceExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    left() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    questionQuestion() -> SyntaxToken {
        return this.tokenAt(1);
    }

    right() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }
}

public class NewExpressionNode extends Expression {
    NewExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    newKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    createdType() -> Type {
        let node = this.nodeAt(1);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 1 of a {this.kind()} node");
    }

    arguments() -> ArgumentListNode? {
        let node = this.nodeAtOrNull(2);
        return node as? ArgumentListNode;
    }

    dimensionsCount() -> long {
        let list = this.nodeAt(3);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 3 of a {this.kind()} node");
    }

    dimensions(long index) -> ArrayDimensionNode {
        let list = this.nodeAt(3);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is ArrayDimensionNode) {
                return node;
            }
        }
        panic("Internal: expected ArrayDimensionNode in the list at slot 3 of a {this.kind()} node");
    }
}

public class ArrayDimensionNode extends SyntaxNode {
    ArrayDimensionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    openBracket() -> SyntaxToken {
        return this.tokenAt(0);
    }

    size() -> Expression? {
        let node = this.nodeAtOrNull(1);
        return node as? Expression;
    }

    closeBracket() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class ParenthesizedExpressionNode extends Expression {
    ParenthesizedExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    openParenthesis() -> SyntaxToken {
        return this.tokenAt(0);
    }

    inner() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }

    closeParenthesis() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class ArrayLiteralExpressionNode extends Expression {
    ArrayLiteralExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    openBracket() -> SyntaxToken {
        return this.tokenAt(0);
    }

    elementsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    elements(long index) -> Expression {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is Expression) {
                return node;
            }
        }
        panic("Internal: expected Expression in the list at slot 1 of a {this.kind()} node");
    }

    closeBracket() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

public class InterpolatedStringExpressionNode extends Expression {
    InterpolatedStringExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    start() -> SyntaxToken {
        return this.tokenAt(0);
    }

    firstHole() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }

    continuationsCount() -> long {
        let list = this.nodeAt(2);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 2 of a {this.kind()} node");
    }

    continuations(long index) -> InterpolationPartNode {
        let list = this.nodeAt(2);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is InterpolationPartNode) {
                return node;
            }
        }
        panic("Internal: expected InterpolationPartNode in the list at slot 2 of a {this.kind()} node");
    }

    end() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

public class InterpolationPartNode extends SyntaxNode {
    InterpolationPartNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    middle() -> SyntaxToken {
        return this.tokenAt(0);
    }

    hole() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }
}

public class TryExpressionNode extends Expression {
    TryExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    tryKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    operand() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }
}

public class SwitchExpressionNode extends Expression {
    SwitchExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    switchKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    scrutinee() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }

    closeParenthesis() -> SyntaxToken {
        return this.tokenAt(3);
    }

    openBrace() -> SyntaxToken {
        return this.tokenAt(4);
    }

    armsCount() -> long {
        let list = this.nodeAt(5);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 5 of a {this.kind()} node");
    }

    arms(long index) -> SwitchArm {
        let list = this.nodeAt(5);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is SwitchArm) {
                return node;
            }
        }
        panic("Internal: expected SwitchArm in the list at slot 5 of a {this.kind()} node");
    }

    closeBrace() -> SyntaxToken {
        return this.tokenAt(6);
    }
}

public class ErrorDeclarationNode extends Declaration implements TopLevelItem, Member {
    ErrorDeclarationNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}

public class ErrorStatementNode extends Statement {
    ErrorStatementNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}

public class ErrorExpressionNode extends Expression {
    ErrorExpressionNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}

public class ErrorTypeNode extends Type {
    ErrorTypeNode(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}
