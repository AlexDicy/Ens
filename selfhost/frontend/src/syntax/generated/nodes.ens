// Generated by syntaxgen from selfhost/syntax.grammar. Do not edit.
import GreenNode from syntax.green;
import NodeFactory from syntax.node;
import SyntaxNode from syntax.node;
import SyntaxToken from syntax.node;

export interface TopLevelItem { }

export interface Member { }

export interface ExternalItem { }

export interface Argument { }

export interface ArmBody { }

export interface ElseBody { }

export interface ForInitializer { }

export sealed abstract class Declaration extends SyntaxNode {
    protected constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

export sealed abstract class Statement extends SyntaxNode {
    protected constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

export sealed abstract class Expression extends SyntaxNode implements Argument, ArmBody {
    protected constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

export sealed abstract class Type extends SyntaxNode {
    protected constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

export sealed abstract class SwitchArm extends SyntaxNode {
    protected constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

export sealed abstract class ManifestDeclaration extends SyntaxNode {
    protected constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

export sealed abstract class PackageItem extends SyntaxNode {
    protected constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

export sealed abstract class WorkspaceItem extends SyntaxNode {
    protected constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

export sealed abstract class OverridesItem extends SyntaxNode {
    protected constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

export sealed abstract class NativeBinding extends SyntaxNode {
    protected constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }
}

// Container for list slots: its children are the list elements, with the separator tokens
// interleaved at the odd indices for separated lists.
export class ListNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export childCount() -> long {
        return this.slotCount();
    }

    export childNodeAt(long index) -> SyntaxNode {
        return this.nodeAt(index);
    }

    export childTokenAt(long index) -> SyntaxToken {
        return this.tokenAt(index);
    }
}

export class SourceFileNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export itemsCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export items(long index) -> TopLevelItem {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is TopLevelItem) {
                return node;
            }
        }
        panic("Internal: expected TopLevelItem in the list at slot 0 of a {this.kind()} node");
    }

    export eof() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

export class ManifestFileNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export declarationsCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export declarations(long index) -> ManifestDeclaration {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is ManifestDeclaration) {
                return node;
            }
        }
        panic("Internal: expected ManifestDeclaration in the list at slot 0 of a {this.kind()} node");
    }

    export eof() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

export class PackageDeclarationNode extends ManifestDeclaration {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export packageKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export name() -> PackagePathNode {
        let node = this.nodeAt(1);
        if (node is PackagePathNode) {
            return node;
        }
        panic("Internal: expected PackagePathNode in slot 1 of a {this.kind()} node");
    }

    export openBrace() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export itemsCount() -> long {
        let list = this.nodeAt(3);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 3 of a {this.kind()} node");
    }

    export items(long index) -> PackageItem {
        let list = this.nodeAt(3);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is PackageItem) {
                return node;
            }
        }
        panic("Internal: expected PackageItem in the list at slot 3 of a {this.kind()} node");
    }

    export closeBrace() -> SyntaxToken {
        return this.tokenAt(4);
    }
}

export class WorkspaceDeclarationNode extends ManifestDeclaration {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export workspaceKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export openBrace() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export itemsCount() -> long {
        let list = this.nodeAt(2);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 2 of a {this.kind()} node");
    }

    export items(long index) -> WorkspaceItem {
        let list = this.nodeAt(2);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is WorkspaceItem) {
                return node;
            }
        }
        panic("Internal: expected WorkspaceItem in the list at slot 2 of a {this.kind()} node");
    }

    export closeBrace() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

export class OverridesDeclarationNode extends ManifestDeclaration {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export overridesKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export openBrace() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export itemsCount() -> long {
        let list = this.nodeAt(2);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 2 of a {this.kind()} node");
    }

    export items(long index) -> OverridesItem {
        let list = this.nodeAt(2);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is OverridesItem) {
                return node;
            }
        }
        panic("Internal: expected OverridesItem in the list at slot 2 of a {this.kind()} node");
    }

    export closeBrace() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

export class PackagePathNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export segmentsCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export segments(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index * 2);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}

export class VersionDeclarationNode extends PackageItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export versionKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export value() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class EnsDeclarationNode extends PackageItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export ensKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export value() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class DependencyDeclarationNode extends PackageItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export dependencyKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export name() -> PackagePathNode {
        let node = this.nodeAt(1);
        if (node is PackagePathNode) {
            return node;
        }
        panic("Internal: expected PackagePathNode in slot 1 of a {this.kind()} node");
    }

    export version() -> SyntaxToken? {
        return this.tokenAtOrNull(2);
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

export class MemberDeclarationNode extends WorkspaceItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export memberKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export folder() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class OverrideDeclarationNode extends OverridesItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export overrideKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export name() -> PackagePathNode {
        let node = this.nodeAt(1);
        if (node is PackagePathNode) {
            return node;
        }
        panic("Internal: expected PackagePathNode in slot 1 of a {this.kind()} node");
    }

    export folder() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

export class NativeDeclarationNode extends PackageItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export nativeKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export systemKeyword() -> SyntaxToken? {
        return this.tokenAtOrNull(2);
    }

    export bindings() -> NativeBindingListNode? {
        let node = this.nodeAtOrNull(3);
        return node as? NativeBindingListNode;
    }

    export semicolon() -> SyntaxToken? {
        return this.tokenAtOrNull(4);
    }
}

export class NativeBindingListNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export openBrace() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export bindingsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export bindings(long index) -> NativeBinding {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is NativeBinding) {
                return node;
            }
        }
        panic("Internal: expected NativeBinding in the list at slot 1 of a {this.kind()} node");
    }

    export closeBrace() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class NativeLibraryBindingNode extends NativeBinding {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export platform() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export namesCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export names(long index) -> SyntaxToken {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class NativeArtifactBindingNode extends NativeBinding {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export platform() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export artifactKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export url() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export hashKeyword() -> SyntaxToken {
        return this.tokenAt(3);
    }

    export checksum() -> SyntaxToken {
        return this.tokenAt(4);
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(5);
    }
}

export class FunctionDeclarationNode extends Declaration implements TopLevelItem, Member {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export modifiersCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export modifiers(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export introducer() -> SyntaxToken? {
        return this.tokenAtOrNull(1);
    }

    export name() -> SyntaxToken? {
        return this.tokenAtOrNull(2);
    }

    export typeParameters() -> TypeParameterListNode? {
        let node = this.nodeAtOrNull(3);
        return node as? TypeParameterListNode;
    }

    export parameters() -> ParameterListNode {
        let node = this.nodeAt(4);
        if (node is ParameterListNode) {
            return node;
        }
        panic("Internal: expected ParameterListNode in slot 4 of a {this.kind()} node");
    }

    export returnType() -> ReturnTypeClauseNode? {
        let node = this.nodeAtOrNull(5);
        return node as? ReturnTypeClauseNode;
    }

    export throwsClause() -> ThrowsClauseNode? {
        let node = this.nodeAtOrNull(6);
        return node as? ThrowsClauseNode;
    }

    export body() -> BlockNode? {
        let node = this.nodeAtOrNull(7);
        return node as? BlockNode;
    }

    export semicolon() -> SyntaxToken? {
        return this.tokenAtOrNull(8);
    }

    export catchClausesCount() -> long {
        let list = this.nodeAt(9);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 9 of a {this.kind()} node");
    }

    export catchClauses(long index) -> CatchClauseNode {
        let list = this.nodeAt(9);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is CatchClauseNode) {
                return node;
            }
        }
        panic("Internal: expected CatchClauseNode in the list at slot 9 of a {this.kind()} node");
    }
}

export class ParameterListNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export openParenthesis() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export parametersCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export parameters(long index) -> ParameterNode {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is ParameterNode) {
                return node;
            }
        }
        panic("Internal: expected ParameterNode in the list at slot 1 of a {this.kind()} node");
    }

    export closeParenthesis() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class ParameterNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export outKeyword() -> SyntaxToken? {
        return this.tokenAtOrNull(0);
    }

    export declaredType() -> Type? {
        let node = this.nodeAtOrNull(1);
        return node as? Type;
    }

    export thisPrefix() -> ThisPrefixNode? {
        let node = this.nodeAtOrNull(2);
        return node as? ThisPrefixNode;
    }

    export name() -> SyntaxToken {
        return this.tokenAt(3);
    }

    export defaultValue() -> InitializerNode? {
        let node = this.nodeAtOrNull(4);
        return node as? InitializerNode;
    }
}

export class ThisPrefixNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export thisKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export dot() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

export class InitializerNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export equals() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export value() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }
}

export class ReturnTypeClauseNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export arrow() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export returnedType() -> Type {
        let node = this.nodeAt(1);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 1 of a {this.kind()} node");
    }
}

export class ThrowsClauseNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export throwsKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export typesCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export types(long index) -> NamedTypeNode {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is NamedTypeNode) {
                return node;
            }
        }
        panic("Internal: expected NamedTypeNode in the list at slot 1 of a {this.kind()} node");
    }
}

export class CatchClauseNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export catchKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export caughtType() -> NamedTypeNode {
        let node = this.nodeAt(2);
        if (node is NamedTypeNode) {
            return node;
        }
        panic("Internal: expected NamedTypeNode in slot 2 of a {this.kind()} node");
    }

    export name() -> SyntaxToken {
        return this.tokenAt(3);
    }

    export closeParenthesis() -> SyntaxToken {
        return this.tokenAt(4);
    }

    export body() -> BlockNode {
        let node = this.nodeAt(5);
        if (node is BlockNode) {
            return node;
        }
        panic("Internal: expected BlockNode in slot 5 of a {this.kind()} node");
    }
}

export class StructDeclarationNode extends Declaration implements TopLevelItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export modifiersCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export modifiers(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export structKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export typeParameters() -> TypeParameterListNode? {
        let node = this.nodeAtOrNull(3);
        return node as? TypeParameterListNode;
    }

    export extendsClause() -> ExtendsClauseNode? {
        let node = this.nodeAtOrNull(4);
        return node as? ExtendsClauseNode;
    }

    export implementsClause() -> ImplementsClauseNode? {
        let node = this.nodeAtOrNull(5);
        return node as? ImplementsClauseNode;
    }

    export body() -> MemberListNode {
        let node = this.nodeAt(6);
        if (node is MemberListNode) {
            return node;
        }
        panic("Internal: expected MemberListNode in slot 6 of a {this.kind()} node");
    }
}

export class ClassDeclarationNode extends Declaration implements TopLevelItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export modifiersCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export modifiers(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export classKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export typeParameters() -> TypeParameterListNode? {
        let node = this.nodeAtOrNull(3);
        return node as? TypeParameterListNode;
    }

    export extendsClause() -> ExtendsClauseNode? {
        let node = this.nodeAtOrNull(4);
        return node as? ExtendsClauseNode;
    }

    export implementsClause() -> ImplementsClauseNode? {
        let node = this.nodeAtOrNull(5);
        return node as? ImplementsClauseNode;
    }

    export body() -> MemberListNode {
        let node = this.nodeAt(6);
        if (node is MemberListNode) {
            return node;
        }
        panic("Internal: expected MemberListNode in slot 6 of a {this.kind()} node");
    }
}

export class InterfaceDeclarationNode extends Declaration implements TopLevelItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export modifiersCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export modifiers(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export interfaceKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export typeParameters() -> TypeParameterListNode? {
        let node = this.nodeAtOrNull(3);
        return node as? TypeParameterListNode;
    }

    export body() -> MemberListNode {
        let node = this.nodeAt(4);
        if (node is MemberListNode) {
            return node;
        }
        panic("Internal: expected MemberListNode in slot 4 of a {this.kind()} node");
    }
}

export class ExtendsClauseNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export extendsKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export base() -> NamedTypeNode {
        let node = this.nodeAt(1);
        if (node is NamedTypeNode) {
            return node;
        }
        panic("Internal: expected NamedTypeNode in slot 1 of a {this.kind()} node");
    }
}

export class ImplementsClauseNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export implementsKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export interfacesCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export interfaces(long index) -> NamedTypeNode {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is NamedTypeNode) {
                return node;
            }
        }
        panic("Internal: expected NamedTypeNode in the list at slot 1 of a {this.kind()} node");
    }
}

export class MemberListNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export openBrace() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export membersCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export members(long index) -> Member {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is Member) {
                return node;
            }
        }
        panic("Internal: expected Member in the list at slot 1 of a {this.kind()} node");
    }

    export closeBrace() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class FieldDeclarationNode extends Declaration implements Member {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export modifiersCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export modifiers(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export declaredType() -> Type {
        let node = this.nodeAt(1);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 1 of a {this.kind()} node");
    }

    export name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export defaultValue() -> InitializerNode? {
        let node = this.nodeAtOrNull(3);
        return node as? InitializerNode;
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(4);
    }
}

export class EnumDeclarationNode extends Declaration implements TopLevelItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export modifiersCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export modifiers(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export enumKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export openBrace() -> SyntaxToken {
        return this.tokenAt(3);
    }

    export membersCount() -> long {
        let list = this.nodeAt(4);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 4 of a {this.kind()} node");
    }

    export members(long index) -> EnumMemberNode {
        let list = this.nodeAt(4);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is EnumMemberNode) {
                return node;
            }
        }
        panic("Internal: expected EnumMemberNode in the list at slot 4 of a {this.kind()} node");
    }

    export closeBrace() -> SyntaxToken {
        return this.tokenAt(5);
    }
}

export class EnumMemberNode extends Declaration {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export value() -> InitializerNode? {
        let node = this.nodeAtOrNull(1);
        return node as? InitializerNode;
    }
}

export class ImportDeclarationNode extends Declaration implements TopLevelItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export importKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export binding() -> ImportBindingNode? {
        let node = this.nodeAtOrNull(1);
        return node as? ImportBindingNode;
    }

    export path() -> ImportPathNode {
        let node = this.nodeAt(2);
        if (node is ImportPathNode) {
            return node;
        }
        panic("Internal: expected ImportPathNode in slot 2 of a {this.kind()} node");
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

export class ImportBindingNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export fromKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

export class ImportPathNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export at() -> SyntaxToken? {
        return this.tokenAtOrNull(0);
    }

    export segmentsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export segments(long index) -> SyntaxToken {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return list.childTokenAt(index * 2);
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }
}

export class ExternalTypeDeclarationNode extends Declaration implements TopLevelItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export externalKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export typeKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

export class ExternalBlockNode extends Declaration implements TopLevelItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export externalKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export fromKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export library() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export openBrace() -> SyntaxToken {
        return this.tokenAt(3);
    }

    export functionsCount() -> long {
        let list = this.nodeAt(4);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 4 of a {this.kind()} node");
    }

    export functions(long index) -> ExternalItem {
        let list = this.nodeAt(4);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is ExternalItem) {
                return node;
            }
        }
        panic("Internal: expected ExternalItem in the list at slot 4 of a {this.kind()} node");
    }

    export closeBrace() -> SyntaxToken {
        return this.tokenAt(5);
    }
}

export class ExternalFunctionDeclarationNode extends Declaration implements ExternalItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export parameters() -> ParameterListNode {
        let node = this.nodeAt(1);
        if (node is ParameterListNode) {
            return node;
        }
        panic("Internal: expected ParameterListNode in slot 1 of a {this.kind()} node");
    }

    export returnType() -> ReturnTypeClauseNode? {
        let node = this.nodeAtOrNull(2);
        return node as? ReturnTypeClauseNode;
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

export class TestDeclarationNode extends Declaration implements TopLevelItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export testKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    // This slot holds either a token or a node; exactly one accessor is non-null.
    export descriptionToken() -> SyntaxToken? {
        return this.tokenAtOrNull(1);
    }

    export descriptionNode() -> InterpolatedStringExpressionNode? {
        let node = this.nodeAtOrNull(1);
        return node as? InterpolatedStringExpressionNode;
    }

    export body() -> BlockNode {
        let node = this.nodeAt(2);
        if (node is BlockNode) {
            return node;
        }
        panic("Internal: expected BlockNode in slot 2 of a {this.kind()} node");
    }
}

export class TypeParameterListNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export less() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export parametersCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export parameters(long index) -> TypeParameterNode {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is TypeParameterNode) {
                return node;
            }
        }
        panic("Internal: expected TypeParameterNode in the list at slot 1 of a {this.kind()} node");
    }

    export greater() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class TypeParameterNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export bounds() -> BoundsClauseNode? {
        let node = this.nodeAtOrNull(1);
        return node as? BoundsClauseNode;
    }
}

export class BoundsClauseNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export colon() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export boundsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export bounds(long index) -> NamedTypeNode {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is NamedTypeNode) {
                return node;
            }
        }
        panic("Internal: expected NamedTypeNode in the list at slot 1 of a {this.kind()} node");
    }
}

export class NamedTypeNode extends Type {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export qualifier() -> TypeQualifierNode? {
        let node = this.nodeAtOrNull(0);
        return node as? TypeQualifierNode;
    }

    export name() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export typeArguments() -> TypeArgumentListNode? {
        let node = this.nodeAtOrNull(2);
        return node as? TypeArgumentListNode;
    }
}

export class TypeQualifierNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export module() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export dot() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

export class ArrayTypeNode extends Type {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export element() -> Type {
        let node = this.nodeAt(0);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 0 of a {this.kind()} node");
    }

    export openBracket() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export closeBracket() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class OptionalTypeNode extends Type {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export inner() -> Type {
        let node = this.nodeAt(0);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 0 of a {this.kind()} node");
    }

    export question() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

export class TypeArgumentListNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export less() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export argumentsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export arguments(long index) -> Type {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is Type) {
                return node;
            }
        }
        panic("Internal: expected Type in the list at slot 1 of a {this.kind()} node");
    }

    export greater() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class BlockNode extends Statement implements ArmBody, ElseBody {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export openBrace() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export statementsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export statements(long index) -> Statement {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is Statement) {
                return node;
            }
        }
        panic("Internal: expected Statement in the list at slot 1 of a {this.kind()} node");
    }

    export closeBrace() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class VariableDeclarationNode extends Statement implements ForInitializer {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export modifier() -> SyntaxToken? {
        return this.tokenAtOrNull(0);
    }

    export declaredType() -> Type? {
        let node = this.nodeAtOrNull(1);
        return node as? Type;
    }

    export name() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export initializer() -> InitializerNode? {
        let node = this.nodeAtOrNull(3);
        return node as? InitializerNode;
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(4);
    }
}

export class IfStatementNode extends Statement implements ElseBody {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export ifKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export condition() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }

    export closeParenthesis() -> SyntaxToken {
        return this.tokenAt(3);
    }

    export thenBranch() -> BlockNode {
        let node = this.nodeAt(4);
        if (node is BlockNode) {
            return node;
        }
        panic("Internal: expected BlockNode in slot 4 of a {this.kind()} node");
    }

    export elseClause() -> ElseClauseNode? {
        let node = this.nodeAtOrNull(5);
        return node as? ElseClauseNode;
    }
}

export class ElseClauseNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export elseKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export body() -> ElseBody {
        let node = this.nodeAt(1);
        if (node is ElseBody) {
            return node;
        }
        panic("Internal: expected ElseBody in slot 1 of a {this.kind()} node");
    }
}

export class WhileStatementNode extends Statement {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export whileKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export condition() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }

    export closeParenthesis() -> SyntaxToken {
        return this.tokenAt(3);
    }

    export body() -> BlockNode {
        let node = this.nodeAt(4);
        if (node is BlockNode) {
            return node;
        }
        panic("Internal: expected BlockNode in slot 4 of a {this.kind()} node");
    }
}

export class ForStatementNode extends Statement {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export forKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export initializer() -> ForInitializer? {
        let node = this.nodeAtOrNull(2);
        return node as? ForInitializer;
    }

    export initializerSemicolon() -> SyntaxToken? {
        return this.tokenAtOrNull(3);
    }

    export condition() -> Expression? {
        let node = this.nodeAtOrNull(4);
        return node as? Expression;
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(5);
    }

    export update() -> Expression? {
        let node = this.nodeAtOrNull(6);
        return node as? Expression;
    }

    export closeParenthesis() -> SyntaxToken {
        return this.tokenAt(7);
    }

    export body() -> BlockNode {
        let node = this.nodeAt(8);
        if (node is BlockNode) {
            return node;
        }
        panic("Internal: expected BlockNode in slot 8 of a {this.kind()} node");
    }
}

export class ForEachStatementNode extends Statement {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export forKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export letKeyword() -> SyntaxToken? {
        return this.tokenAtOrNull(2);
    }

    export declaredType() -> Type? {
        let node = this.nodeAtOrNull(3);
        return node as? Type;
    }

    export name() -> SyntaxToken {
        return this.tokenAt(4);
    }

    export inKeyword() -> SyntaxToken {
        return this.tokenAt(5);
    }

    export iterable() -> Expression {
        let node = this.nodeAt(6);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 6 of a {this.kind()} node");
    }

    export closeParenthesis() -> SyntaxToken {
        return this.tokenAt(7);
    }

    export body() -> BlockNode {
        let node = this.nodeAt(8);
        if (node is BlockNode) {
            return node;
        }
        panic("Internal: expected BlockNode in slot 8 of a {this.kind()} node");
    }
}

export class BreakStatementNode extends Statement {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export breakKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

export class ContinueStatementNode extends Statement {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export continueKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

export class ReturnStatementNode extends Statement {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export returnKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export value() -> Expression? {
        let node = this.nodeAtOrNull(1);
        return node as? Expression;
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class ExpressionStatementNode extends Statement implements ForInitializer {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export expression() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

export class ThrowStatementNode extends Statement {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export throwKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export value() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class RethrowStatementNode extends Statement {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export rethrowKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export semicolon() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

export class SwitchStatementNode extends Statement {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export switchKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export scrutinee() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }

    export closeParenthesis() -> SyntaxToken {
        return this.tokenAt(3);
    }

    export openBrace() -> SyntaxToken {
        return this.tokenAt(4);
    }

    export armsCount() -> long {
        let list = this.nodeAt(5);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 5 of a {this.kind()} node");
    }

    export arms(long index) -> SwitchArm {
        let list = this.nodeAt(5);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is SwitchArm) {
                return node;
            }
        }
        panic("Internal: expected SwitchArm in the list at slot 5 of a {this.kind()} node");
    }

    export closeBrace() -> SyntaxToken {
        return this.tokenAt(6);
    }
}

export class ValueArmNode extends SwitchArm {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export labelsCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export labels(long index) -> Expression {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is Expression) {
                return node;
            }
        }
        panic("Internal: expected Expression in the list at slot 0 of a {this.kind()} node");
    }

    export arrow() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export body() -> ArmBody {
        let node = this.nodeAt(2);
        if (node is ArmBody) {
            return node;
        }
        panic("Internal: expected ArmBody in slot 2 of a {this.kind()} node");
    }
}

export class TypeArmNode extends SwitchArm {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export isKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export target() -> NamedTypeNode {
        let node = this.nodeAt(1);
        if (node is NamedTypeNode) {
            return node;
        }
        panic("Internal: expected NamedTypeNode in slot 1 of a {this.kind()} node");
    }

    export binding() -> SyntaxToken? {
        return this.tokenAtOrNull(2);
    }

    export arrow() -> SyntaxToken {
        return this.tokenAt(3);
    }

    export body() -> ArmBody {
        let node = this.nodeAt(4);
        if (node is ArmBody) {
            return node;
        }
        panic("Internal: expected ArmBody in slot 4 of a {this.kind()} node");
    }
}

export class DefaultArmNode extends SwitchArm {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export defaultKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export arrow() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export body() -> ArmBody {
        let node = this.nodeAt(2);
        if (node is ArmBody) {
            return node;
        }
        panic("Internal: expected ArmBody in slot 2 of a {this.kind()} node");
    }
}

export class LiteralExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export value() -> SyntaxToken {
        return this.tokenAt(0);
    }
}

export class IdentifierExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(0);
    }
}

export class ThisExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export thisKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }
}

export class SuperExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export superKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }
}

export class BinaryExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export left() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export operator() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export right() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }
}

export class PrefixExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export operator() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export operand() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }
}

export class PostfixExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export operand() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export operator() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

export class CallExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export callee() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export typeArguments() -> TypeArgumentListNode? {
        let node = this.nodeAtOrNull(1);
        return node as? TypeArgumentListNode;
    }

    export arguments() -> ArgumentListNode {
        let node = this.nodeAt(2);
        if (node is ArgumentListNode) {
            return node;
        }
        panic("Internal: expected ArgumentListNode in slot 2 of a {this.kind()} node");
    }
}

export class ArgumentListNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export openParenthesis() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export argumentsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export arguments(long index) -> Argument {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is Argument) {
                return node;
            }
        }
        panic("Internal: expected Argument in the list at slot 1 of a {this.kind()} node");
    }

    export closeParenthesis() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class OutArgumentNode extends SyntaxNode implements Argument {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export outKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(1);
    }
}

export class NamedArgumentNode extends SyntaxNode implements Argument {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export colon() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export value() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }
}

export class MemberExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export object() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export dot() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export member() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class SafeMemberExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export object() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export questionDot() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export member() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class SubscriptExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export object() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export openBracket() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export index() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }

    export closeBracket() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

export class SafeSubscriptExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export object() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export question() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export openBracket() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export index() -> Expression {
        let node = this.nodeAt(3);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 3 of a {this.kind()} node");
    }

    export closeBracket() -> SyntaxToken {
        return this.tokenAt(4);
    }
}

export class CastExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export value() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export asKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export target() -> Type {
        let node = this.nodeAt(2);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 2 of a {this.kind()} node");
    }
}

export class CheckedCastExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export value() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export asKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export question() -> SyntaxToken {
        return this.tokenAt(2);
    }

    export target() -> Type {
        let node = this.nodeAt(3);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 3 of a {this.kind()} node");
    }
}

export class TypeTestExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export value() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export isKeyword() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export target() -> NamedTypeNode {
        let node = this.nodeAt(2);
        if (node is NamedTypeNode) {
            return node;
        }
        panic("Internal: expected NamedTypeNode in slot 2 of a {this.kind()} node");
    }
}

export class AssignmentExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export target() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export operator() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export value() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }
}

export class TernaryExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export condition() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export question() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export thenValue() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }

    export colon() -> SyntaxToken {
        return this.tokenAt(3);
    }

    export elseValue() -> Expression {
        let node = this.nodeAt(4);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 4 of a {this.kind()} node");
    }
}

export class NullCoalesceExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export left() -> Expression {
        let node = this.nodeAt(0);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 0 of a {this.kind()} node");
    }

    export questionQuestion() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export right() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }
}

export class NewExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export newKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export createdType() -> Type {
        let node = this.nodeAt(1);
        if (node is Type) {
            return node;
        }
        panic("Internal: expected Type in slot 1 of a {this.kind()} node");
    }

    export arguments() -> ArgumentListNode? {
        let node = this.nodeAtOrNull(2);
        return node as? ArgumentListNode;
    }

    export dimensionsCount() -> long {
        let list = this.nodeAt(3);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 3 of a {this.kind()} node");
    }

    export dimensions(long index) -> ArrayDimensionNode {
        let list = this.nodeAt(3);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is ArrayDimensionNode) {
                return node;
            }
        }
        panic("Internal: expected ArrayDimensionNode in the list at slot 3 of a {this.kind()} node");
    }
}

export class ArrayDimensionNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export openBracket() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export size() -> Expression? {
        let node = this.nodeAtOrNull(1);
        return node as? Expression;
    }

    export closeBracket() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class ParenthesizedExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export openParenthesis() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export inner() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }

    export closeParenthesis() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class ArrayLiteralExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export openBracket() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export elementsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export elements(long index) -> Expression {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is Expression) {
                return node;
            }
        }
        panic("Internal: expected Expression in the list at slot 1 of a {this.kind()} node");
    }

    export closeBracket() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class StructLiteralExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export openBrace() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export fieldsCount() -> long {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 1 of a {this.kind()} node");
    }

    export fields(long index) -> StructLiteralFieldNode {
        let list = this.nodeAt(1);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is StructLiteralFieldNode) {
                return node;
            }
        }
        panic("Internal: expected StructLiteralFieldNode in the list at slot 1 of a {this.kind()} node");
    }

    export closeBrace() -> SyntaxToken {
        return this.tokenAt(2);
    }
}

export class StructLiteralFieldNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export name() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export colon() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export value() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }
}

export class InterpolatedStringExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export start() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export firstHole() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }

    export continuationsCount() -> long {
        let list = this.nodeAt(2);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 2 of a {this.kind()} node");
    }

    export continuations(long index) -> InterpolationPartNode {
        let list = this.nodeAt(2);
        if (list is ListNode) {
            let node = list.childNodeAt(index);
            if (node is InterpolationPartNode) {
                return node;
            }
        }
        panic("Internal: expected InterpolationPartNode in the list at slot 2 of a {this.kind()} node");
    }

    export end() -> SyntaxToken {
        return this.tokenAt(3);
    }
}

export class InterpolationPartNode extends SyntaxNode {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export middle() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export hole() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }
}

export class TryExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export tryKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export operand() -> Expression {
        let node = this.nodeAt(1);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 1 of a {this.kind()} node");
    }
}

export class SwitchExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export switchKeyword() -> SyntaxToken {
        return this.tokenAt(0);
    }

    export openParenthesis() -> SyntaxToken {
        return this.tokenAt(1);
    }

    export scrutinee() -> Expression {
        let node = this.nodeAt(2);
        if (node is Expression) {
            return node;
        }
        panic("Internal: expected Expression in slot 2 of a {this.kind()} node");
    }

    export closeParenthesis() -> SyntaxToken {
        return this.tokenAt(3);
    }

    export openBrace() -> SyntaxToken {
        return this.tokenAt(4);
    }

    export armsCount() -> long {
        let list = this.nodeAt(5);
        if (list is ListNode) {
            return (list.childCount() + 1) / 2;
        }
        panic("Internal: expected a List in slot 5 of a {this.kind()} node");
    }

    export arms(long index) -> SwitchArm {
        let list = this.nodeAt(5);
        if (list is ListNode) {
            let node = list.childNodeAt(index * 2);
            if (node is SwitchArm) {
                return node;
            }
        }
        panic("Internal: expected SwitchArm in the list at slot 5 of a {this.kind()} node");
    }

    export closeBrace() -> SyntaxToken {
        return this.tokenAt(6);
    }
}

export class ErrorDeclarationNode extends Declaration implements TopLevelItem, Member, ExternalItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}

export class ErrorStatementNode extends Statement {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}

export class ErrorExpressionNode extends Expression {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}

export class ErrorTypeNode extends Type {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}

export class ErrorManifestDeclarationNode extends ManifestDeclaration {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}

export class ErrorPackageItemNode extends PackageItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}

export class ErrorWorkspaceItemNode extends WorkspaceItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}

export class ErrorOverridesItemNode extends OverridesItem {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}

export class ErrorNativeBindingNode extends NativeBinding {
    public constructor(GreenNode green, long fullStart, SyntaxNode? parent, NodeFactory factory) {
        super(green, fullStart, parent, factory);
    }

    export skippedCount() -> long {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childCount();
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }

    export skipped(long index) -> SyntaxToken {
        let list = this.nodeAt(0);
        if (list is ListNode) {
            return list.childTokenAt(index);
        }
        panic("Internal: expected a List in slot 0 of a {this.kind()} node");
    }
}
