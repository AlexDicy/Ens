// Abort for broken internal invariants. These paths mark bugs in the tree code
// or its generated accessors; no input program can reach them.
external from "c" {
    exit(int code);
}

public fatal(string message) {
    print("Internal: {message}");
    exit(134);
}
