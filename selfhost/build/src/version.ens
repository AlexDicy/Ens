// The version of the toolchain this executable is. It is the language version the compiler in
// this binary implements, which is what a package's 'ens' declaration names, so a build root
// declaring the same version is one this toolchain is written for.

export final class Version {
    export static const string toolchain = "0.1";
}
