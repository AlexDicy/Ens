// The one path helper the sema layer needs beyond @std.path: joining a range of parts, which
// serves both file paths and the dotted module paths built from the same segment lists.
import StringBuilder from @std.text.stringbuilder;

// Joins parts[from..to) with the separator.
public joinRange(string[] parts, string separator, long from, long to) -> string {
    let builder = new StringBuilder();
    for (long i = from; i < to; i++) {
        if (i > from) {
            builder.append(separator);
        }
        builder.append(parts[i]);
    }
    return builder.toString();
}
