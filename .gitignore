# IDEs
.idea/
*.iml

.vscode/*
!.vscode/launch.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/extensions.json
!.vscode/*.code-snippets
.history/
*.vsix

# xmake
.xmake/
# the build output folders, named so that the ens.build package folder is not one of them
/build/
tools/intellij/build/
cmake-build-debug/
Makefile
makefile
CMakeLists.txt
Xmake.Custom.*
vsxmake20*/*
!vsxmake20*/*.sln.DotSettings

# macOS
.DS_Store

# LLM tools
.claude/*.local.json

# generated files
*.gen.cpp

# local package overrides (test fixtures excepted)
ens.overrides
!tests/**/ens.overrides
