// @expect-error '~' flips the bits of an integer, got 'string'
// A string carries text, not a number, so it has nothing for '~' to work on.
main() -> int {
    string label = "flags";
    let flipped = ~label;
    print("{flipped}");
    return 0;
}
