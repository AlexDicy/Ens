// A module that declares 'print(string)' itself. The declaration takes over the one the language
// provides, so every unqualified call here reaches it, and it is an ordinary public function that
// another module calls by qualifying it.
import emit;

public print(string message) {
    emit.line("own: " + message);
}

public speak(string message) {
    print(message);
}
