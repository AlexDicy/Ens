// @exit 134
// @stderr-contains panic: popFront from an empty deque: check isEmpty() first
// Popping an empty deque aborts in the deque's own words, naming the end the caller popped.
import Deque from @std.collections.deque;

main() -> int {
    let empty = new Deque<int>();
    return empty.popFront();
}
