// @expect-error The constructor of 'Vault' is private, so only code inside 'Vault' can call it. Mark it 'public' to construct 'Vault' here.
// @expect-error The constructor of 'Seal' is private, so only code inside 'Seal' can call it. Mark it 'public' to construct 'Seal' here.
// @expect-error build failed with 2 problems
// An unmarked constructor of a 'public' class or struct is private to that type, so even the
// declaring file cannot construct it.
public class Vault {
    constructor();
}

public struct Seal {
    int code;

    constructor(this.code);
}

main() -> int {
    let vault = new Vault();
    let seal = Seal(1);
    return 0;
}
