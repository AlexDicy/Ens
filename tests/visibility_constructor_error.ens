// @expect-error The constructor of 'Vault' is private and can only be called from inside 'Vault'
// An unmarked constructor is private to its type, so even the declaring file
// cannot construct the class.
class Vault {
    constructor();
}

main() -> int {
    let vault = new Vault();
    return 0;
}
