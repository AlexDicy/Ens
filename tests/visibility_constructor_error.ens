// @expect-error The constructor of 'Vault' is private, so only code inside 'Vault' can call it. Mark it 'public' to construct 'Vault' here.
// An unmarked constructor is private to its type, so even the declaring file
// cannot construct the class.
class Vault {
    constructor();
}

main() -> int {
    let vault = new Vault();
    return 0;
}
