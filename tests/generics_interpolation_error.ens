// @expect-error Cannot interpolate a value of type '(int) -> int'
// A generic that interpolates its type parameter is rejected when an instance takes a type with
// no text form, judged per instantiation like the struct '==' rule. A class instantiation is
// accepted since class values render from their runtime type, and an array since arrays render
// as their contents; a function value stays refused.
class Point {
    int x;
    protected constructor(this.x);
}

describe<T>(T value) -> string {
    return "value is {value}";
}

main() -> int {
    let p = new Point(1);
    print(describe(p));
    print(describe([1, 2]));
    (int) -> int doubler = (v) -> v * 2;
    print(describe(doubler));
    return 0;
}
