// @expect-error can throw 'Error', which interface 'Quiet' does not allow
interface Quiet {
    run() -> int;
}

class Noisy implements Quiet {
    constructor();
    run() -> int throws {
        throw new Error("boom");
    }
}

main() -> int {
    return 0;
}
