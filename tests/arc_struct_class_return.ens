// @exit 7
class Box {
    int v;
    constructor(this.v = 0);
}

struct Container {
    Box b = new Box();
}

makeContainer() -> Container {
    Container c = {};
    c.b.v = 7;
    return c;
}

main() -> int {
    let result = makeContainer();
    return result.b.v;
}
