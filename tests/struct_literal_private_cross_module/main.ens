// @expect-error is private to
// A struct literal respects field visibility: the private field of an imported
// struct cannot be set from another module.
import Point from shapes;

main() -> int {
    Point p = {secret: 1, x: 2};
    return p.x;
}
