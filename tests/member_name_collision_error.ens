// @expect-error a field and a method cannot share a name
class Resource {
    int state;
    constructor(this.state = 0);
    state() -> int { return this.state; }   // collides with the 'state' field
}
main() -> int { return 0; }