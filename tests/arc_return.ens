// @exit 5
class Box {
    int value;
    Box(this.value = 0);
}

makeBox() -> Box {
    let b = new Box();
    b.value = 5;
    return b;
}

main() -> int {
    let result = makeBox();
    return result.value;
}
