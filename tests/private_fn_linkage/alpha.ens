helper() -> long {
    return 5;
}

describe(string first) -> string {
    return "alpha says " + first;
}

public wrapperAlpha() -> long {
    return helper();
}

public sayAlpha(string first) -> string {
    return describe(first);
}
