helper() -> long {
    return 7;
}

describe(string first, string second) -> string {
    return "beta says " + first + " " + second;
}

public wrapperBeta() -> long {
    return helper();
}

public sayBeta(string first, string second) -> string {
    return describe(first, second);
}
