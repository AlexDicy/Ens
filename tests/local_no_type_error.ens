// @expect-error Variable 'unset' has no declared type, so its type has to come from an initializer. Write 'let unset = ...;', or declare the type.
// @expect-error Variable 'missing' has no declared type, so its type has to come from an initializer. Write 'let missing = ...;', or declare the type.
// @expect-error build failed with 2 problems
// A local written without a type takes it from its initializer, so one with neither has nothing
// to be. 'const' reads the same way: the keyword says how often it may be assigned, not what it
// holds.
main() -> int {
    let unset;
    const missing;
    return 0;
}
