// @expect-error declares no version; a dependency is versionless only when it resolves to a workspace member
// Only member dependencies are versionless; an override redirects a declared requirement.
main() -> int {
    return 0;
}
