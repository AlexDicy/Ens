// @expect-error declares no version, and only a dependency that resolves to a workspace member may leave it out
// Only member dependencies are versionless; an override redirects a declared requirement.
main() -> int {
    return 0;
}
