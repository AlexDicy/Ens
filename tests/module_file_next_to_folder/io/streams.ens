public class Stream {
    public string name;

    public constructor(this.name);
}

public standard() -> Stream {
    return new Stream("buffered stream");
}

public describe(Stream stream) -> string {
    return stream.name;
}
