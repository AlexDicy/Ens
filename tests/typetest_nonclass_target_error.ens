// @expect-error The target of 'is' must be a class or an interface, got 'int'
class Box {
    int v;
    Box(this.v = 0);
}

main() -> int {
    Box b = new Box(1);
    if (b is int) { return 1; }
    return 0;
}
