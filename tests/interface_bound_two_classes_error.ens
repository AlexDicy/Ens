// @expect-error Type parameter 'T' already has the class bound 'Animal'; at most one bound can be a class, every other bound must be an interface
class Animal {
    constructor();
}

class Robot {
    constructor();
}

class Pen<T: Animal + Robot> {
    private T occupant;
    constructor(this.occupant);
}

main() -> int {
    return 0;
}
