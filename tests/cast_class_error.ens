// @expect-error 'as' converts between numbers, and between an enum and an integer
class Box {
    int v;
    protected constructor(this.v = 0);
}

main() -> int {
    Box b = new Box(1);
    int x = b as int;
    return x;
}
