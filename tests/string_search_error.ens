// @expect-error No overload of 'indexOf' matches this call. Candidates: 'indexOf(string)', 'indexOf(string, long)'.
// String search takes a string needle; other argument types match neither overload.
main() -> int {
    let text = "hello";
    print(text.indexOf(5).toString());
    return 0;
}
