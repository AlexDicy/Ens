// @expect-error 'indexOf' expects a string argument, got 'int'
// String search takes a string needle; other argument types are rejected.
main() -> int {
    let text = "hello";
    print(text.indexOf(5).toString());
    return 0;
}
