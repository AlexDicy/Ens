// @exit 134
// @stderr-contains panic: repeat count -1 is negative
// Repeating a negative number of times is a broken contract rather than empty text.
main() -> int {
    let text = "ab";
    return text.repeat(-1).length as int;
}
