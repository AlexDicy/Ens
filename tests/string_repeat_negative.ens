// @exit 134
// @stderr-contains panic: tried to repeat a string -1 times, and a count cannot be negative. Check the count first
// Repeating a negative number of times is a broken contract rather than empty text.
main() -> int {
    let text = "ab";
    return text.repeat(-1).length as int;
}
