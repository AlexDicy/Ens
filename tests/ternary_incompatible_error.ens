// @expect-error The two results of this '?:' are 'int' and 'string', which have no common type. Give both branches the same type.
main() -> int {
    bool c = true;
    let x = c ? 5 : "five";
    return 0;
}
