// @expect-error The members of an interface always share the interface's visibility
// Interface members carry no visibility of their own.
interface Speaker {
    public speak() -> string;
}

main() -> int {
    return 0;
}
