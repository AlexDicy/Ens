// @expect-error A member of an interface always has the interface's own visibility
// Interface members carry no visibility of their own.
interface Speaker {
    public speak() -> string;
}

main() -> int {
    return 0;
}
