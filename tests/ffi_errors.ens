// @expect-error 'out' can only be used when calling an external function.
external from "c" {
    abs(int x) -> int;
}

regular(int n) -> int {
    return n;
}

main() -> int {
    let x: int = 0;
    // out is only valid on external functions.
    let y = regular(out x);
    return abs(y);
}
