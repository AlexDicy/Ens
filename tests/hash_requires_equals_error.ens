// @expect-error Class 'Coordinate' defines 'hash' but not 'equals', and equal values have to hash equally. Add 'override equals(Coordinate other) -> bool'.
class Coordinate {
    int x;
    constructor(this.x);
    override hash() -> long { return this.x; }
}

main() -> int {
    return 0;
}
