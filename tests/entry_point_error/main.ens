// @expect-error so only the main module (src/main.ens) can define it
// Only the main module of a program may define main(); 'helper' also defines it.
import helper;

main() -> int {
    return helper.value();
}
