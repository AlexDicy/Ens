// @expect-error Function 'main' starts the program, so only the program's main module, 'main.ens', can declare it. This file is module 'helper'. Rename this function, or move it into 'main.ens'.
// Only the main module of a program may define main(); 'helper' also defines it.
import helper;

main() -> int {
    return helper.value();
}
