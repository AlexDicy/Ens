// @expect-error Namespace alias 'kinds' conflicts with an earlier import in this file.
// @expect-error Imported name 'Kind' conflicts with an earlier import in this file.
// @expect-error Imported name 'Sort' conflicts with an earlier import in this file.
// @expect-error Function name 'pick' is already used in this file by an import of the type 'beta.kinds.Kind'; rename the function, or bind the import to another name with 'as'.
// @expect-error Function 'where' cannot be imported by name. Import the module instead: 'import alpha.tools;' then call it as 'tools.where'.
// @expect-error 'class' is a keyword and cannot be used as an import alias; choose a different name
// @expect-error Function name 'shapes' is already used in this file by an import of the module 'beta.kinds'; rename the function, or bind the import to another name with 'as'.
// @expect-error Imported name 'Wheel' is already declared as a class in this file; bind the import to another name, as in 'import Kind as LibWheel from beta.kinds;'.
// @expect-error Imported name 'Frame' is already declared as a struct in this file; bind the import to another name, as in 'import Kind as LibFrame from beta.kinds;'.
// @expect-error Imported name 'Rider' is already declared as an interface in this file; bind the import to another name, as in 'import Kind as LibRider from beta.kinds;'.
// @expect-error Imported name 'Gear' is already declared as an enum in this file; bind the import to another name, as in 'import Kind as LibGear from beta.kinds;'.
// @expect-error Imported name 'Handle' is already declared as an external type in this file; bind the import to another name, as in 'import Kind as LibHandle from beta.kinds;'.
// @expect-error Imported module name 'spanner' is already declared as a class in this file; bind the import to another name, as in 'import alpha.tools as spannerModule;'.
// @expect-error build failed with 13 problems
// What a name an import binds may not clash with: a name another import binds, a function or a
// type the file declares, or a keyword. A named import cannot rename a function either.
import alpha.kinds;
import beta.kinds as kinds;
import Kind from alpha.kinds;
import Other as Kind from beta.kinds;
import Kind as Sort from alpha.kinds;
import Kind as Sort from beta.kinds;
import Kind as pick from beta.kinds;
import beta.kinds as shapes;
import where as locate from alpha.tools;
import alpha.tools as class;
import Kind as Wheel from beta.kinds;
import Kind as Frame from beta.kinds;
import Kind as Rider from beta.kinds;
import Kind as Gear from beta.kinds;
import Kind as Handle from beta.kinds;
import alpha.tools as spanner;

pick() -> int {
    return 1;
}

shapes() -> int {
    return 2;
}

class Wheel {
    protected constructor();
}

struct Frame {
    protected int width;
}

interface Rider {
}

enum Gear {
    Low,
}

external type Handle;

class spanner {
    protected constructor();
}

main() -> int {
    return 0;
}
