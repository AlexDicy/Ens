// @expect-error Namespace alias 'kinds' conflicts with an earlier import in this file.
// @expect-error Imported name 'Kind' conflicts with an earlier import in this file.
// @expect-error Imported name 'Sort' conflicts with an earlier import in this file.
// @expect-error Function name 'pick' is already used in this file by an import of the type 'beta.kinds.Kind'; rename the function, or bind the import to another name with 'as'.
// @expect-error Function 'where' cannot be imported by name. Import the module instead: 'import alpha.tools;' then call it as 'tools.where'.
// @expect-error 'class' is a keyword and cannot be used as an import alias; choose a different name
// @expect-error Function name 'shapes' is already used in this file by an import of the module 'beta.kinds'; rename the function, or bind the import to another name with 'as'.
// @expect-error build failed with 7 problems
// What an alias may not do: take a name another import binds, take a name the file declares,
// rename a function import, or be a keyword.
import alpha.kinds;
import beta.kinds as kinds;
import Kind from alpha.kinds;
import Other as Kind from beta.kinds;
import Kind as Sort from alpha.kinds;
import Kind as Sort from beta.kinds;
import Kind as pick from beta.kinds;
import beta.kinds as shapes;
import where as locate from alpha.tools;
import alpha.tools as class;

pick() -> int {
    return 1;
}

shapes() -> int {
    return 2;
}

main() -> int {
    return 0;
}
