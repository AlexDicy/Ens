// @exit 7
class Box {
    int value;
    constructor(this.value = 0);
}

addOne(Box b) {
    b.value = b.value + 1;
}

main() -> int {
    let x = new Box();
    x.value = 6;
    addOne(x);
    return x.value;
}
