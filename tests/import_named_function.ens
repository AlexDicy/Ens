// @expect-error Function 'openFile' cannot be imported by name
// Free functions may only be used namespace-qualified, never imported by name.
import openFile from @std.system;

main() -> int {
    return 0;
}
