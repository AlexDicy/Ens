// @expect-error Function 'arguments' cannot be imported by name
// Free functions may only be used namespace-qualified, never imported by name.
import arguments from @std.environment;

main() -> int {
    return 0;
}
