// @expect-error Function 'arguments' cannot be imported by name. Import the module instead: 'import @std.environment;' then call it as 'environment.arguments'.
// @expect-error build failed with 1 problem
// Free functions may only be used namespace-qualified, never imported by name. The refused import
// is the one problem, and a call through the name is not reported again.
import arguments from @std.environment;

main() -> int {
    let given = arguments();
    return 0;
}
