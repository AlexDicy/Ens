// @expect-error Duplicate bound 'Speaker' on type parameter 'T'; list each bound once
interface Speaker {
    speak() -> string;
}

class Chorus<T: Speaker + Speaker> {
    private T lead;
    constructor(this.lead);
}

main() -> int {
    return 0;
}
