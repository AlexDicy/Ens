// @expect-error Type parameter 'T' already has the bound 'Speaker'. Remove this second 'Speaker'.
interface Speaker {
    speak() -> string;
}

class Chorus<T: Speaker + Speaker> {
    T lead;
    constructor(this.lead);
}

main() -> int {
    return 0;
}
