// @expect-error Structs cannot implement interfaces; only classes can use 'implements'
interface Speaker {
    speak() -> string;
}

struct Point implements Speaker {
    int x;
    int y;
}

main() -> int {
    return 0;
}
