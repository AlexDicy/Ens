export abstract class Reported {
    string message;
    Reported? cause;

    protected constructor(this.message, this.cause);

    export format() -> string {
        if (this.cause == null) {
            return this.message;
        }
        return this.message + ": " + this.cause.format();
    }
}
