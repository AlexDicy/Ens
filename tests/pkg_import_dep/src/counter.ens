export class Counter {
    long count;

    export constructor();

    export add(long amount) {
        this.count = this.count + amount;
    }

    export value() -> long {
        return this.count;
    }

    protected seed() -> long {
        return 7;
    }
}

export struct Badge {
    string label;
    int stars;
}
