// @ens-test
// @exit 0
// @stdout PASS one, the zeros and the smallest double have the patterns IEEE spells
// @stdout PASS one, the zeros and the smallest float have the patterns IEEE spells
// @stdout PASS a NaN keeps the payload it was built from
// @stdout PASS every finite double comes back out of its bits unchanged
// @stdout PASS every finite float comes back out of its bits unchanged
// @stdout PASS the pattern one exponent above the numbers is an infinity
// @stdout 6/6 tests passed
// The bridges that read a floating-point value's bits and put them back, exercised on the host
// running the suite. The bits of a value are the same on every host, so every pattern the tests
// name is written out rather than read off this machine.

main() -> int {
    return 0;
}
