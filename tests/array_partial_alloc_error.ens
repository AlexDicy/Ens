// @expect-error Sized dimensions must come before any '[]' dimensions
// @expect-error 'new int[...]' needs at least one size. Write 'new int[size]' for one row, or 'new int[size][]' for a grid whose rows are filled later.
// A sized dimension may not follow an unsized one, and at least one dimension carries a size.
main() -> int {
    int[]?[][] grid = new int[3][][2];
    int[] unsized = new int[];
    return 0;
}
