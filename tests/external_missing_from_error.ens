// @expect-error Expected 'from' after 'external'
// With 'from' absent after 'external', the external block still reports the
// missing keyword cleanly instead of misparsing the library string.
external "c" {
    abs(int x) -> int;
}

main() -> int {
    return 0;
}
