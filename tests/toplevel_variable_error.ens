// @expect-error Top-level variables are not supported: 'PI' must be declared inside a function.
// Ens has no top-level variables; a `const`, `let`, or typed declaration at file
// scope is reported at the declaration itself.
const PI = 3;

main() -> int {
    return 0;
}
