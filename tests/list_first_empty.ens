// @exit 134
// @stderr-contains panic: tried to read the first element of an empty list. Check isEmpty() first
// The other end of the same rule, in a program of its own because an abort ends the one it
// happens in.
import List from @std.collections.list;

main() -> int {
    let empty = new List<int>();
    return empty.first();
}
