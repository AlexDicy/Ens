// @expect-error does not handle subclass(es) 'Neg', 'Add' of sealed class 'Expr'
sealed abstract class Expr { Expr(); }
class Num extends Expr { Num() { super(); } }
class Neg extends Expr { Neg() { super(); } }
class Add extends Expr { Add() { super(); } }

main() -> int {
    Expr e = new Num();
    switch (e) {
        is Num -> { },
    }
    return 0;
}
