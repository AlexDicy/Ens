// @exit 134
// @stderr-contains panic: pop from an empty queue: check isEmpty() first
// Popping an empty priority queue aborts in the queue's own words.
import PriorityQueue from @std.collections.priorityqueue;

main() -> int {
    let empty = new PriorityQueue<int>((a, b) -> a - b);
    return empty.pop();
}
