// @expect-error initialize it in every constructor
class Inner {
    int v;
    Inner(this.v = 0);
}

class Outer {
    Inner inner;   // not initialized by any constructor
}

main() -> int {
    Outer o = new Outer();
    return 0;
}