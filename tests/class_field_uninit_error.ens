// @expect-error add a constructor that assigns it
class Inner {
    int v;
    constructor(this.v = 0);
}

class Outer {
    Inner inner;   // not initialized by any constructor
}

main() -> int {
    Outer o = new Outer();
    return 0;
}