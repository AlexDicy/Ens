// A second standard-library module reaching a binding member that is 'public' rather than
// 'export': inside the package it is nameable, and only what this module exports crosses out.
import @std.text.binding;

export sizeOf(string value) -> long {
    return value.inPackageOnly();
}
