// A second module asking for standard input, so the one shared reader is shown to be shared across
// a module boundary rather than within a single expression.
import @std.io;
import BufferedReader from @std.io.buffered;

export reader() -> BufferedReader {
    return io.in();
}
