// @exit 0
// @stdout entry point application
// An application package: src/main.ens is the main module and defines the entry point.
import greeting;

main() -> int {
    print(greeting.text());
    return 0;
}
