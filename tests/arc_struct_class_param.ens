// @exit 100
class Box {
    int v;
    Box(this.v = 0);
}

struct Container {
    Box b = new Box();
}

modify(Container c) {
    c.b = new Box();
    c.b.v = 999;
}

main() -> int {
    Container c;
    c.b.v = 100;
    modify(c);
    return c.b.v;
}
