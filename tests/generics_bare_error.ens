// @expect-error Generic type 'Box' requires type arguments
class Box<T> { private T value; protected constructor(T v) { this.value = v; } }

main() -> int {
    Box b = new Box<int>(5);
    return 0;
}