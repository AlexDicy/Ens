// @expect-error External library 'zlib' is not declared in
// An external block may only name a native library the package manifest declares.
external from zlib {
    compressBound(ulong sourceLength) -> ulong;
}

main() -> int {
    return 0;
}
