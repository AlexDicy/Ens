// @expect-error ', which declares package 'demo.main' instead; the names must match exactly.
// @expect-error The override for package 'acme.tools' points at '
// An override's target folder must declare exactly the overridden package name.
main() -> int {
    return 0;
}
