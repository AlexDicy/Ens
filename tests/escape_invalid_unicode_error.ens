// @expect-error Invalid Unicode escape sequence '\u': write four hex digits
// A \u escape needs exactly four hex digits: reject a non-hex digit and a
// sequence that is truncated before four digits.
main() -> int {
    let nonHex = "\uZZZZ";
    let truncated = "\u12";
    return 0;
}
