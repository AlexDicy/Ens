// @expect-error is not public
// Private declarations are not reachable from another module: the named import of the
// private type and the namespace-qualified call to the private function both fail.
import Secret from lib.secret;
import lib.secret;

main() -> int {
    return secret.hidden();
}
