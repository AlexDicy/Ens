// @exit 11
class Box {
    protected int v;
    protected constructor(this.v = 0);
}

useBoth(Box a, Box b) -> int {
    let x = a;
    x = b;
    return x.v;
}

main() -> int {
    let p = new Box();
    let q = new Box();
    p.v = 7;
    q.v = 11;
    return useBoth(p, q);
}
