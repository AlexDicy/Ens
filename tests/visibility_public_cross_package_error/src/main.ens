// @expect-error Function 'tag' is public inside package 'acme.tools' but not exported
// Public reaches every module of the declaring package but stops at the package
// boundary; only exported declarations are visible to consuming packages.
import @acme.tools.text;

main() -> int {
    print(text.tag());
    return 0;
}
