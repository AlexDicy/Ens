// @exit 2
// @stdout imported Green 2
import Color from lib.palette;

main() -> int {
    const Color c = Color.Green;
    let code = switch (c) {
        Red -> 1,
        Green -> 2,
        Blue -> 3,
    };
    print("imported {c} {code}");
    return code;
}