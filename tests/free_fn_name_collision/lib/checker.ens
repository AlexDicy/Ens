// Exports a public `checkProgram` with the same name and signature as the one
// in `parser`, plus a `tag` that shares its name but not its signature.
public checkProgram() -> string {
    return "checker: checked";
}

public tag() -> string {
    return "checker tag";
}
