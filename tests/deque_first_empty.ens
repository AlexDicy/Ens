// @exit 134
// @stderr-contains panic: tried to read the first element of an empty deque. Check isEmpty() first
// The other end of the same rule, in a program of its own.
import Deque from @std.collections.deque;

main() -> int {
    let empty = new Deque<int>();
    return empty.first();
}
