// @expect-error is declared 'final' in
class Base {
    final greet() -> int { return 1; }
}

class Derived extends Base {
    override greet() -> int { return 2; }
}

main() -> int {
    return 0;
}