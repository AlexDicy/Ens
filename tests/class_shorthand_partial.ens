// @exit 50
class Box {
    protected int width;
    protected int height;
    protected constructor(this.width, this.height = 10);
}

main() -> int {
    Box b = new Box(5);
    return b.width * b.height;
}
