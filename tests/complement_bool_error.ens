// @expect-error '~' flips the bits of an integer, got 'bool'
// '~' is not a spelling of '!': a 'bool' is negated with '!', and '~' asks for an integer.
main() -> int {
    bool ready = true;
    let flipped = ~ready;
    if (flipped) { return 1; }
    return 0;
}
