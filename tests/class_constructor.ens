// @exit 84
class Box {
    int value;
    protected constructor(int initial) {
        this.value = initial;
    }
    protected getDoubled() -> int {
        return this.value * 2;
    }
}

main() -> int {
    Box b = new Box(42);
    return b.getDoubled();
}
