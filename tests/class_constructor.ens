// @exit 84
class Box {
    int value;
    protected constructor(int initial) {
        this.value = initial;
    }
    protected doubled() -> int {
        return this.value * 2;
    }
}

main() -> int {
    Box b = new Box(42);
    return b.doubled();
}
