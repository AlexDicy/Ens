// @exit 84
class Box {
    int value;
    Box(int initial) {
        this.value = initial;
    }
    getDoubled() -> int {
        return this.value * 2;
    }
}

main() -> int {
    Box b = new Box(42);
    return b.getDoubled();
}
