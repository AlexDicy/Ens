// @exit 0
external type Handle;

external from "c" {
    abs(int x) -> int;
}

private isNullHandle(Handle? h) -> bool {
    return h == null;
}

main() -> int {
    let h: Handle? = null;
    if (isNullHandle(h)) {
        return abs(0);
    }
    return abs(-1);
}
