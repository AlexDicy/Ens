// @exit 0
external type Handle;

external from "c" {
    abs(int x) -> int;
}

isNullHandle(Handle? h) -> bool {
    return h == null;
}

main() -> int {
    Handle? h = null;
    if (isNullHandle(h)) {
        return abs(0);
    }
    return abs(-1);
}
