// @expect-error Class 'Silent' implements 'Speaker' but does not provide 'speak(int) -> string'
interface Speaker {
    speak(int volume) -> string;
}

class Silent implements Speaker {
    constructor();
    speak() -> string { return "..."; }
}

main() -> int {
    return 0;
}
