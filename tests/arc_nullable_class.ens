// @exit 0
class Inner {
    int v;
    protected constructor(this.v = 0);
}

class Outer {
    protected Inner? i;
    protected constructor(this.i = null);
}

main() -> int {
    let o = new Outer();
    o.i = new Inner();
    o.i = new Inner();
    o.i = null;
    Inner? b = new Inner();
    Inner? n = null;
    return 0;
}
