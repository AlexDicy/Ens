// @exit 0
class Inner {
    int v;
    Inner(this.v = 0);
}

class Outer {
    Inner? i;
    Outer(this.i = null);
}

main() -> int {
    let o = new Outer();
    o.i = new Inner();
    o.i = new Inner();
    o.i = null;
    let b: Inner? = new Inner();
    let n: Inner? = null;
    return 0;
}
