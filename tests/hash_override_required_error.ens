// @expect-error overrides the built-in identity hash; mark it 'override'
class Widget {
    int id;
    constructor(this.id);
    hash() -> long { return this.id; }
    override equals(Widget other) -> bool { return this.id == other.id; }
}

main() -> int {
    return 0;
}
