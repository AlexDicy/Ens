// @expect-error Type 'Secret' is private to module 'lib.secret', so only that file can use it. Mark it 'public' to use it from other modules in the package.
// @expect-error build failed with 1 problem
// The refused import is the one problem, however often the file uses the name.
import Secret from lib.secret;

keep(Secret value) -> Secret? {
    Secret? local = null;
    return local;
}

main() -> int {
    let made = new Secret();
    return 0;
}
