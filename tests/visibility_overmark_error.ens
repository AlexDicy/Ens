// @expect-error a member cannot be more visible than the type that contains it
// Over-marking is rejected, never silently capped.
public class Counter {
    long count;

    export bump() {
        this.count = this.count + 1;
    }
}

main() -> int {
    return 0;
}
