// @expect-error a member is never more visible than the type that holds it
// Over-marking is rejected, never silently capped.
public class Counter {
    long count;

    export bump() {
        this.count = this.count + 1;
    }
}

main() -> int {
    return 0;
}
