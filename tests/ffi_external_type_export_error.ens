// @expect-error it can never be exported
// An external type may be private or public, but never exported: a foreign handle has no
// meaning outside the package that binds it. Wrap it in an Ens type to share it further.
export external type Handle;

main() -> int {
    return 0;
}
