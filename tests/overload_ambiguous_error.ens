// @expect-error Call to 'pick' is ambiguous. Candidates: 'pick(long)', 'pick(double)'.
pick(long v) -> string { return "long"; }
pick(double v) -> string { return "double"; }

main() -> int {
    pick(5);
    return 0;
}
