// @expect-error An array of 'Box', which cannot hold null, must be filled as it is created
class Box {
    int v;
    constructor(this.v = 0);
}

main() -> int {
    Box[] boxes = new Box[3];
    return 0;
}
