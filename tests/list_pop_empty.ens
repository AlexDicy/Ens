// @exit 134
// @stderr-contains panic: pop from an empty list
// Popping an empty list aborts in the list's own words, naming what the caller did rather than
// the slot the backing store would have been asked for.
import List from @std.collections.list;

main() -> int {
    let empty = new List<int>();
    return empty.pop();
}
