// @exit 134
// @stderr-contains panic: array index out of bounds
// Popping an empty list should panic via the runtime bounds check, like an out-of-range get.
import List from @std.collections.list;

main() -> int {
    let empty = new List<int>();
    return empty.pop();
}
