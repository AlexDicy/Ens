// A module of the stand-in library that declares neither of the names in play, so the 'panic' it
// would call is the language's own and the 'print' it does call is the prelude's.
export line(string text) {
    print(text);
}
