// @exit 0
// @stdout eq-literal-concat
// @stdout neq
// @stdout len=11
// @stdout foobar!?
// @stdout 42 -7 0 true false
// @stdout bytes-roundtrip
// @stdout area = 100 for 20x5
// @stdout nested inner-5
// @stdout count: 5 ok=true
// Happy-path coverage for the string operations: content equality (including a
// literal compared to a concatenation result), length, concatenation chains,
// .toString() across signs and bool, byte[] round-tripping, interpolation, and
// implicit conversion of integer and bool operands in concatenation.
main() -> int {
    let a = "foo";
    let b = "bar";
    if (("foo" + "bar") == "foobar") { print("eq-literal-concat"); }
    if (a != b) { print("neq"); }
    print("len=" + "hello world".length.toString());
    print(a + b + "!" + "?");
    print(42 + " " + -7 + " " + 0 + " " + true + " " + false);
    let bytes = "data".toBytes();
    if (string.fromBytes(bytes) == "data") { print("bytes-roundtrip"); }
    let w = 20;
    let h = 5;
    print("area = {w * h} for {w}x{h}");
    print("nested {"inner-{h}"}");
    let count = 5;
    let ok = true;
    print("count: " + count + " ok=" + ok);
    return 0;
}
