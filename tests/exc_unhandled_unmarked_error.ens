// @expect-error can throw 'DatabaseError' but is not marked 'throws'
class DatabaseError extends Error { protected constructor(string m) { super(m); } }
class Repo {
    query() -> int throws { throw new DatabaseError("x"); }
    caller() -> int { return try this.query(); }   // neither caught nor 'throws'
}
main() -> int { return 0; }