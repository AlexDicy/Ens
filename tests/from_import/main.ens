// @exit 15
// The import-binding form still parses even when the module is literally named
// `from`: `import Gadget from from;` binds Gadget from the module `from`.
import Gadget from from;

main() -> int {
    let gadget = new Gadget(15);
    return gadget.size;
}
