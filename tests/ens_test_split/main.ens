// @ens-test --tests {dir}/tests
// @exit 0
// @stdout PASS greeting composes
// @stdout 1/1 tests passed
// Tests live outside the source tree: `--tests` names their folder, and their
// imports resolve against the source root.
import Greeting from lib.greeting;

main() -> int {
    print(new Greeting().compose("world"));
    return 0;
}
