// @expect-error Duplicate switch label
enum Color { Red, Green, Blue, }

main() -> int {
    const Color c = Color.Red;
    switch (c) {
        Red -> { },
        Red -> { },
        Green -> { },
        Blue -> { },
    }
    return 0;
}