// @expect-error Cannot mix value labels with 'is' arms
sealed abstract class Expr { protected constructor(); }
class Num extends Expr { protected constructor() { super(); } }

main() -> int {
    Expr e = new Num();
    switch (e) {
        is Num -> { },
        5 -> { },
        default -> { },
    }
    return 0;
}
