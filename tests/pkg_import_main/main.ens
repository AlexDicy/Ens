// @stdout Hello, Ada! [acme.tools]
// Imports a module from the @acme.tools package, a dependency declared in ens.package and
// resolved by name against the enclosing workspace's members. The package's own greeter
// imports its sibling by bare name, proving a package's internal imports resolve to the
// same modules as the external @package import.
import @acme.tools.greeter;

main() -> int {
    print(greeter.greet("Ada"));
    return 0;
}
