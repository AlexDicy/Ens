// @exit 134
// @stderr-contains panic: tried to read the last element of an empty deque. Check isEmpty() first
// Asking an empty deque for its last element aborts in the deque's own words.
import Deque from @std.collections.deque;

main() -> int {
    let empty = new Deque<int>();
    return empty.last();
}
