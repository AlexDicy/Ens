// @exit 0
struct Rect {
    int width = 4;
    int height = 5;
    int area = width * height;
}

main() -> int {
    Rect r = {};
    if (r.area != 20) { return 1; }

    // Per-slot init through the array path.
    Rect[] rs = new Rect[2];
    if (rs[0].area != 20) { return 2; }
    if (rs[1].area != 20) { return 3; }
    return 0;
}
