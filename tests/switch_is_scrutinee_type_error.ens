// @expect-error use a 'default' arm instead
sealed abstract class Expr { constructor(); }
class Num extends Expr { constructor() { super(); } }

main() -> int {
    Expr e = new Num();
    switch (e) {
        is Expr -> { },       // matches everything: that is what default is for
        is Num -> { },
    }
    return 0;
}
