// @expect-error does not handle enum 'Color' member 'Blue'. Add it or a 'default' arm.
enum Color { Red, Green, Blue, }

main() -> int {
    const Color c = Color.Red;
    switch (c) {
        Red -> { },
        Green -> { },
    }
    return 0;
}