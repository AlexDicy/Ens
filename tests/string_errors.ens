// @expect-error Cannot use '+' to combine
// Strings do not implicitly absorb other types: '+' requires both sides to be
// strings, so the value must be converted with .toString() (or interpolated).
main() -> int {
    let count = 5;
    print("count: " + count);
    return 0;
}
