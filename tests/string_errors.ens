// @expect-error Cannot concatenate a value of type 'double'
// Integer and bool operands convert to text implicitly, but a type without a
// string conversion yet (here, double) is still rejected by '+'.
main() -> int {
    let ratio = 3.14;
    print("ratio: " + ratio);
    return 0;
}
