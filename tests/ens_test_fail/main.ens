// @ens-test
// @exit 1
// @stdout-contains PASS subtraction handles negatives
// @stdout-contains FAIL subtraction fails on purpose: expected 1, got -1
// @stdout-contains at assertEqual (testing.ens:
// @stdout-contains at "subtraction fails on purpose" (math_test.ens:
// @stdout-contains PASS custom failure messages surface
// @stdout-contains 2/3 tests passed
// One failing test neither stops the run nor hides the summary; the report
// names the failing test, its message, and the throw site.
