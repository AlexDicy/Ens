// @expect-error Invalid escape sequence '\q': valid escapes include \n, \t, and \\
// Unknown backslash escapes are rejected in string literals, in char literals,
// and for the C-style \x that Ens does not support.
main() -> int {
    let s = "a\qb";
    let c = '\w';
    let hex = "\x41";
    return 0;
}
