// @exit 1
// @stdout Unhandled exception NetworkError: connection refused
// A `throws` main whose exception escapes: the compiler-emitted entry wrapper
// prints the type and message, then exits 1.
class NetworkError extends Error {
    NetworkError(string m) { super(m); }
}
main() -> int throws {
    throw new NetworkError("connection refused");
}