// @expect-error is marked 'override' but no base class declares it
class Base {
    greet() -> int { return 1; }
}

class Derived extends Base {
    override speak() -> int { return 2; }
}

main() -> int {
    return 0;
}