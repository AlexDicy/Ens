// @expect-error Interface 'Speaker' cannot declare field 'volume'. An interface lists only the methods a type must provide. Remove the field, or move it into a class that implements 'Speaker'.
interface Speaker {
    int volume;
    speak() -> string;
}

main() -> int {
    return 0;
}
