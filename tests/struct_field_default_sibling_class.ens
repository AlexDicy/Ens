// @exit 7
class Box {
    protected int v;
    protected constructor(this.v = 0);
}

struct Pair {
    Box first = new Box(7);
    protected Box second = first;
}

main() -> int {
    Pair p = {};
    return p.second.v;
}
