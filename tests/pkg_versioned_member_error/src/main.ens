// @expect-error Dependency 'acme.tools' resolves to the workspace member at '
// @expect-error '. A workspace member is always used from its folder, so it takes no version. Remove the version '2.0'.
// A dependency that resolves to a workspace member must not carry a version.
main() -> int {
    return 0;
}
