// @expect-error Native library 'zlib' is declared with different bindings
// Two packages in one build must agree on a native library's binding.
import @demo.pkga.lefttag;
import @demo.pkgb.righttag;

main() -> int {
    print(lefttag.tag() + righttag.tag());
    return 0;
}
