// @expect-error 'Rock' is 'final' and does not implement it
interface Speaker {
    speak() -> string;
}

final class Rock {
    Rock();
}

main() -> int {
    Rock r = new Rock();
    if (r is Speaker) {
        return 1;
    }
    return 0;
}
