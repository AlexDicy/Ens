// @exit 100
class Box {
    int value;
    Box(this.value = 0);
}

main() -> int {
    let a = new Box();
    let b = new Box();
    let c = new Box();
    a.value = 30;
    b.value = 30;
    c.value = 40;
    return a.value + b.value + c.value;
}
