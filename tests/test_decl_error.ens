// @expect-error Test declarations are only allowed in files ending '_test.ens'.
// A test declaration outside a test file is rejected.
test "misplaced test" {
    let x = 1;
}

main() -> int {
    return 0;
}
