// @expect-error is not iterable: it does not implement 'Iterable' from '@std.collections.iterator'
class Opaque {
    int value;
    constructor(this.value);
}

main() -> int {
    let box = new Opaque(1);
    for (let item in box) {
        print("" + item);
    }
    return 0;
}
