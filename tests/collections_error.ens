// @expect-error is not iterable: it has no 'makeIterator()' method
class Opaque {
    int value;
    Opaque(this.value);
}

main() -> int {
    let box = new Opaque(1);
    for (let item in box) {
        print("" + item);
    }
    return 0;
}
