// @expect-error The destructor of 'Handle' cannot take parameters. No code calls a destructor, so nothing could pass them. Write it as 'destructor()'.
// A destructor takes no parameters.
class Handle {
    int id = 0;
    destructor(int extra) {
    }
}

main() -> int {
    return 0;
}
