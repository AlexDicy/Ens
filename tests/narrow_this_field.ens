// @exit 42
// `this.field` narrowing
class Shape {
    int area;
    constructor(this.area = 0);
}

class Drawing {
    Shape? shape;
    constructor(this.shape = null);

    areaOrZero() -> int {
        if (this.shape != null) {
            return this.shape.area;
        }
        return 0;
    }
}

main() -> int {
    let d = new Drawing(new Shape(42));
    let empty = new Drawing();
    return d.areaOrZero() + empty.areaOrZero();
}