// @exit 134
// @stderr-contains panic: tried to pop from the back of an empty deque. Check isEmpty() first
// The other end of the rule deque_pop_empty pins, which names the end the caller popped.
import Deque from @std.collections.deque;

main() -> int {
    let empty = new Deque<int>();
    return empty.popBack();
}
