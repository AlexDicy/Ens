// @exit 7
class Child {
    int v;
    weak Parent? parent;
    constructor(this.v = 0);
}

class Parent {
    Child? child;
    constructor(this.child = null);
}

main() -> int {
    let p = new Parent();
    let c = new Child();
    c.v = 7;
    p.child = c;
    c.parent = p;
    return c.v;
}
