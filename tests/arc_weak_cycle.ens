// @exit 7
class Child {
    int v;
    weak Parent? parent;
    Child(this.v = 0);
}

class Parent {
    Child? child;
    Parent(this.child = null);
}

main() -> int {
    let p = new Parent();
    let c = new Child();
    c.v = 7;
    p.child = c;
    c.parent = p;
    return c.v;
}
