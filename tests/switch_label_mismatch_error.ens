// @expect-error 'Square' is not a member of enum 'Shape'
enum Shape { Circle, Triangle, }

main() -> int {
    const Shape s = Shape.Circle;
    switch (s) {
        Circle -> { },
        Square -> { },
        Triangle -> { },
    }
    return 0;
}