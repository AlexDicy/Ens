// @expect-error Dependency 'acme.tools' resolves to the workspace member at '
// @expect-error '. A workspace member is always used from its folder, so it takes no 'from' clause. Remove the 'from' clause.
// A dependency that resolves to a workspace member must not name a git source.
main() -> int {
    return 0;
}
