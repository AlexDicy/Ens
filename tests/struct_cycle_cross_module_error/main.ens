// @expect-error Struct 'Outer' contains itself by value: field 'nested' of 'Outer' has type 'Inner', and field 'back' of 'Inner' has type 'Outer'
// A by-value struct cycle is impossible to lay out even when it crosses a
// module boundary.
import Inner from inner;

public struct Outer {
    Inner nested;
    long tail = 0;
}

main() -> int {
    return 0;
}
