// @expect-error A method named 'equals' that takes a 'Gadget' must return 'bool'
class Gadget {
    int id;
    constructor(this.id);
    override hash() -> long { return this.id; }
    equals(Gadget other) -> int { return this.id - other.id; }
}

main() -> int {
    return 0;
}
