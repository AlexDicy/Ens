// @exit 99
class Counter {
    int value;
    constructor(this.value = 0);
    bump(int by = 11) -> int {
        this.value = this.value + by;
        return this.value;
    }
}

main() -> int {
    Counter c = new Counter(33);
    c.bump();
    c.bump(55);
    return c.value;
}
