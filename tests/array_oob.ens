// @exit 134
// @stderr-contains panic: tried to read index 5 of an array of length 3. Check the index against length first
// A read past the end aborts, and the panic names the index it was given and the length it had to
// stay under.
main() -> int {
    int[] xs = new int[3];
    return xs[5];
}
