// @expect-error takes a value of an external type
// fromCString reads a foreign handle, so a non-external argument (here a string) is rejected.
import @std.ffi;

main() -> int {
    let s = ffi.fromCString("not a handle");
    return s.length as int;
}
