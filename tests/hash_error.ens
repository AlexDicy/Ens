// @expect-error A method named 'hash' must have the signature 'hash() -> long'
class Badge {
    int number;
    Badge(this.number);
    hash(int seed) -> int { return this.number + seed; }
}

main() -> int {
    return 0;
}
