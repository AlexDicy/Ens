public class DbError extends Error {
    protected constructor(string m) { super(m); }
}
public class Db {
    public constructor();
    public query(int q) -> int throws {
        throw new DbError("query failed");
    }
}