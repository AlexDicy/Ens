// @expect-error A value of type 'Animal' can never be of type 'Plant', so this 'is' test is always false. Remove it.
class Animal {
    protected constructor();
}

class Plant {
    constructor();
}

main() -> int {
    Animal a = new Animal();
    if (a is Plant) { return 1; }
    return 0;
}
