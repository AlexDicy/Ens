// @expect-error A value of type 'Animal' can never be a 'Plant'
class Animal {
    constructor();
}

class Plant {
    constructor();
}

main() -> int {
    Animal a = new Animal();
    if (a is Plant) { return 1; }
    return 0;
}
