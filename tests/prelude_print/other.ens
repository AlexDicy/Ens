// A module claiming neither name, so every call here reaches the prelude's.
public speak(string message) {
    print("other: " + message);
    eprint("other: " + message);
}

public record(string text) {
    print(text);
}
