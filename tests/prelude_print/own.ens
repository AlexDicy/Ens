// A module declaring 'print' itself. The prelude lends a name only where nothing nearer claims
// it, so every unqualified call here reaches this declaration instead.
import other;

public print(string message) {
    other.record("own: " + message);
}

public speak(string message) {
    print(message);
}
