// @ens-test
// @exit 0
// @stdout PASS addition adds small integers
// @stdout PASS lists grow through pushes
// @stdout PASS objects allocated in a test body are reclaimed
// @stdout PASS loop allocations are reclaimed
// @stdout PASS description formats the value
// @stdout PASS search finds the needle
// @stdout 6/6 tests passed
// An all-passing project: `ens test` discovers both test modules (sorted by
// path, tests in source order), never compiles this entry point, and exits 0.
import Calculator from lib.calculator;

main() -> int {
    let calculator = new Calculator();
    print("sum is {calculator.add(2, 3)}");
    return 0;
}
