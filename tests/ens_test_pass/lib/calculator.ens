public class Calculator {
    public constructor();

    public add(int left, int right) -> int {
        return left + right;
    }

    public describe(int value) -> string {
        return "value {value}";
    }
}
