import @std.testing;
import Calculator from lib.calculator;

test "description formats the value" {
    let calculator = new Calculator();
    try testing.assertEqual(calculator.describe(9), "value 9");
}

test "search finds the needle" {
    try testing.assertTrue("hello, world".contains("world"));
    try testing.assertEqual("hello, world".indexOf("world"), 7L);
}
