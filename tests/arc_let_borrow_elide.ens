// @exit 42
class Box {
    int v;
    constructor(this.v = 0);
}

readViaLet(Box b) -> int {
    let x = b;
    return x.v;
}

readViaTyped(Box b) -> int {
    Box x = b;
    return x.v;
}

main() -> int {
    let bx = new Box();
    bx.v = 21;
    return readViaLet(bx) + readViaTyped(bx);
}
