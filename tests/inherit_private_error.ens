// @expect-error is private to class
class Base {
    int secret;
    constructor(this.secret = 0);
}

class Derived extends Base {
    reveal() -> int { return this.secret; }
}

main() -> int {
    return 0;
}