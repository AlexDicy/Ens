// @expect-error Type argument 'Grow' for type parameter 'U' of 'Container' must implement interface 'Small'. Replace 'Grow' with a type that implements 'Small', or add 'implements Small' to the declaration of 'Grow'.
interface Small {
    tiny() -> bool;
}

class Grow {
    constructor();
}

class Holder<T: Container<Grow>> {
    constructor();
}

interface Container<U: Small> {
    get() -> U;
}

main() -> int {
    return 0;
}
