// @expect-error Class 'Coordinate' defines 'equals' but not 'hash', and equal values have to hash equally. Add 'override hash() -> long'.
class Coordinate {
    int x;
    constructor(this.x);
    override equals(Coordinate other) -> bool { return this.x == other.x; }
}

main() -> int {
    return 0;
}
