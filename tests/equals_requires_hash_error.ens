// @expect-error defines 'equals' but not 'hash'
class Coordinate {
    int x;
    constructor(this.x);
    override equals(Coordinate other) -> bool { return this.x == other.x; }
}

main() -> int {
    return 0;
}
