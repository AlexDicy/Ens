// @expect-error implements a method declared in interface 'Greeter'; mark it 'override'
interface Greeter {
    greet() -> string;
}

class Person implements Greeter {
    Person();
    greet() -> string { return "hi"; }
}

main() -> int {
    return 0;
}
