// @expect-error A struct cannot declare a destructor
// Destructors are a class-only feature; structs keep only the synthesized field release.
struct Point {
    int x = 0;
    destructor() {
    }
}

main() -> int {
    return 0;
}
