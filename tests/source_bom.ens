// A leading byte order mark is skipped: this file starts with one, the way a Windows editor
// saves it. The mark sits in front of this comment, so every directive below starts its own
// line and the test harnesses read them as usual.
// @exit 0
// @stdout marked source compiles
main() -> int {
    print("marked source compiles");
    return 0;
}
