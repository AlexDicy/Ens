// @exit 42
class Box {
    protected int v;
    protected constructor(this.v = 0);
}

struct Container {
    protected Box b = new Box();
}

readAlias(Container c) -> int {
    let x = c;
    return x.b.v;
}

main() -> int {
    Container c = {};
    c.b.v = 42;
    return readAlias(c);
}
