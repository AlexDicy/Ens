// @exit 42
class Box {
    int v;
    constructor(this.v = 0);
}

struct Container {
    Box b = new Box();
}

readAlias(Container c) -> int {
    let x = c;
    return x.b.v;
}

main() -> int {
    Container c;
    c.b.v = 42;
    return readAlias(c);
}
