import Vector from @std.vector;

public contribute() -> int {
    let v = new Vector<int>();
    v.push(3);
    v.push(4);
    return (v.get(0) as int) + (v.get(1) as int);
}

public pick<T>(T a, T b, bool first) -> T {
    if (first) { return a; }
    return b;
}