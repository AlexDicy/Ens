// @expect-error which is private to module
// A declaration's signature may not mention a type less visible than the
// declaration itself.
class Secret {
    protected constructor();
}

public reveal() -> Secret {
    return new Secret();
}

main() -> int {
    return 0;
}
