// @expect-error An 'ens.package' file declares a package or a workspace. Overrides go in the 'ens.overrides' file next to it.
// Overrides live in the untracked ens.overrides file, never in ens.package.
main() -> int {
    return 0;
}
