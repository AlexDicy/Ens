// @expect-error Package 'acme.widgets' is not available here: these sources sit under the workspace at '
// @expect-error ens.package' but not inside a member package, so only '@std' is importable.
import @acme.widgets;

main() -> int {
    return 0;
}
