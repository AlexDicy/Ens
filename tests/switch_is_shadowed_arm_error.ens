// @expect-error This 'is Dot' arm is unreachable
sealed abstract class Shape { protected constructor(); }
class Circle extends Shape { protected constructor() { super(); } }
class Dot extends Circle { protected constructor() { super(); } }
class Square extends Shape { constructor() { super(); } }

main() -> int {
    Shape s = new Dot();
    switch (s) {
        is Circle -> { },
        is Dot -> { },        // already matched by the Circle arm above
        is Square -> { },
    }
    return 0;
}
