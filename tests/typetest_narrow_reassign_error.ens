// @expect-error No field or method 'radius' on type 'Shape'
class Shape {
    constructor();
}

class Circle extends Shape {
    int radius;
    constructor(this.radius = 0);
}

main() -> int {
    Shape s = new Circle(5);
    if (s is Circle) {
        s = new Shape();
        int r = s.radius;   // the reassignment dropped the narrowing
    }
    return 0;
}
