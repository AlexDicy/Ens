// @exit 50
class Box {
    int v;
    Box(this.v = 0);
}

struct Container {
    Box b = new Box();
}

readSum(Container c) -> int {
    return c.b.v + c.b.v;
}

main() -> int {
    Container c;
    c.b.v = 25;
    return readSum(c);
}
