// A second file aliasing the same modules under other names: an alias is file-local, and a type
// stays one type however each file spells it.
import sizes.kinds as sizes;
import colors.kinds as colors;

public describe(sizes.Kind size, colors.Kind color) -> string {
    return "{sizes.label(size)} {colors.label(color)}";
}
