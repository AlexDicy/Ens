// @expect-error No source for package 'ghost.lib'
// A dependency resolves to a workspace member, an override, or a git source named
// with 'from'; anything else fails the build.
main() -> int {
    return 0;
}
