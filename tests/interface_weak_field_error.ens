// @expect-error Field 'watched' of class 'Watcher' is 'weak', so its type must be a class. 'Speaker' is an interface. Replace 'Speaker' with a class that implements 'Speaker'.
interface Speaker {
    speak() -> string;
}

class Watcher {
    weak Speaker? watched;
    constructor();
}

main() -> int {
    return 0;
}
