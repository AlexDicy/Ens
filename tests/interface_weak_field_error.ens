// @expect-error A 'weak' field must refer to a class, not to an interface
interface Speaker {
    speak() -> string;
}

class Watcher {
    weak Speaker? watched;
    constructor();
}

main() -> int {
    return 0;
}
