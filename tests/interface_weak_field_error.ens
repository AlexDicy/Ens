// @expect-error 'weak' fields must reference a class; interface-typed weak fields are not supported
interface Speaker {
    speak() -> string;
}

class Watcher {
    weak Speaker? watched;
    Watcher();
}

main() -> int {
    return 0;
}
