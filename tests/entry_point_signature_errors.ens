// @expect-error so nothing calls it with type arguments and it cannot declare the type parameter 'T'
// @expect-error takes no parameters, so it cannot declare 'limit'
// Nothing calls the entry point with type arguments or values: the operating system starts it
// with none, and a program reads its command line through '@std.environment'.
main<T>(int limit) -> int {
    return limit;
}
