// @expect-error Function 'main' starts the program, so only the program's main module, 'main.ens', can declare it. This file is the main module of package 'acme.entry', a dependency of this program. Rename this function, or remove 'import @acme.entry.main;' from module 'main'.
// Importing a dependency's main module loads a second entry point, which the
// entry-point rule rejects: only this program's main module may define main().
import @acme.entry.main;

main() -> int {
    return 0;
}
