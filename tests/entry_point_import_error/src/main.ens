// @expect-error module 'acme.entry.main' cannot define it
// Importing a dependency's main module loads a second entry point, which the
// entry-point rule rejects: only this program's main module may define main().
import @acme.entry.main;

main() -> int {
    return 0;
}
