// @expect-error '>>' needs an integer shift distance, got 'double'
// A shift distance counts bit positions, so it is an integer even when the value shifted is one.
main() -> int {
    int word = 64;
    double places = 2.0;
    let moved = word >> places;
    return moved;
}
