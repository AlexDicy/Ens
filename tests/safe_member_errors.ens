// @expect-error The value on the left of '?.' has type 'Leaf', which can never be null
class Leaf {
    Leaf? next;
    constructor(this.next = null);
}

main() -> int {
    let leaf = new Leaf();
    // '?.' on a non-optional receiver is rejected.
    let bad = leaf?.next;
    return 0;
}
