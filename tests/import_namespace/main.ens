// @exit 84
// One module import and one name import, which is how a file reaches both a module function and
// a type the module declares.
import Box from lib.box;
import lib.box;

main() -> int {
    Box b = new Box(21);
    return box.doubled(b.doubled());
}
