class Box {
    int value;
    constructor(int initial) {
        this.value = initial;
    }
    getDoubled() -> int {
        return this.value * 2;
    }
}

doubled(int v) -> int {
    return v * 2;
}
