// @expect-error The '++' operator can only change a variable, a field, or an array element
// '++' changes its operand in place, so it needs an assignable target. The
// literal '5' has no storage to update.
main() -> int {
    5++;
    return 0;
}
