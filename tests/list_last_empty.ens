// @exit 134
// @stderr-contains panic: tried to read the last element of an empty list. Check isEmpty() first
// Asking an empty list for its last element aborts in the list's own words.
import List from @std.collections.list;

main() -> int {
    let empty = new List<int>();
    return empty.last();
}
