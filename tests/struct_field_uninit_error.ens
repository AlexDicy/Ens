// @expect-error and no default value
class Inner {
    int v;
    constructor(this.v = 0);
}

struct Wrap {
    Inner inner;   // no default, non-nullable - can't zero-init safely
}

main() -> int {
    Wrap w;
    return 0;
}