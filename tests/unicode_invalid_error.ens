// @expect-error is not valid UTF-8 text: the byte at offset
// This source is intentionally corrupted to exercise the UTF-8 check.
// The byte 0xC3 on the next line has no continuation byte after it.
main() -> int { let bad = "Ã oops"; return 0; }
