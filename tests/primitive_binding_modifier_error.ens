// A primitive binding is introduced by 'primitive' alone; how far a member reaches is its own
// modifier's business.
// @expect-error A primitive binding takes no modifiers; write 'primitive string { ... }' and let each member's own modifier decide how far it reaches
// @expect-error build failed with 1 problem
export primitive int {
}

main() -> int {
    return 0;
}
