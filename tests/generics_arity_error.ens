// @expect-error Generic type 'Box' expects 1 type argument, but 2 were given
class Box<T> { private T value; Box(T v) { this.value = v; } }

main() -> int {
    let b = new Box<int, int>(5);
    return 0;
}