// @expect-error External function 'compute' has the same name as another external function. A native library function is looked up by its name alone, so two functions cannot share one. Remove one of the two declarations.
external from m {
    compute(int x) -> int;
    compute(long x) -> long;
}

main() -> int {
    return 0;
}
