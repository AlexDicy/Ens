// @expect-error overrides the built-in identity equality; mark it 'override'
class Widget {
    int id;
    constructor(this.id);
    override hash() -> long { return this.id; }
    equals(Widget other) -> bool { return this.id == other.id; }
}

main() -> int {
    return 0;
}
