// @expect-error The target of 'as?' cannot be nullable
class Base {
    protected constructor();
}

class Derived extends Base {
    protected constructor();
}

main() -> int {
    Base b = new Derived();
    let d = b as? Derived?;
    return 0;
}
