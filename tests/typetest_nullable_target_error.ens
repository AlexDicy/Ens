// @expect-error The target of 'as?' cannot be nullable
class Base {
    constructor();
}

class Derived extends Base {
    constructor();
}

main() -> int {
    Base b = new Derived();
    let d = b as? Derived?;
    return 0;
}
