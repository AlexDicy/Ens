// @expect-error this 'is' test would always be true
class Base {
    Base();
}

class Derived extends Base {
    Derived();
}

main() -> int {
    Derived d = new Derived();
    if (d is Base) { return 1; }
    return 0;
}
