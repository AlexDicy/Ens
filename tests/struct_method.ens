// @exit 42
struct Rectangle {
    protected int width;
    protected int height;
    protected area() -> int {
        return this.width * this.height;
    }
}

main() -> int {
    Rectangle r = {width: 6, height: 7};
    return r.area();
}
