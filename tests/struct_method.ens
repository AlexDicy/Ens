// @exit 42
struct Rectangle {
    int width;
    int height;
    getArea() -> int {
        return this.width * this.height;
    }
}

main() -> int {
    Rectangle r = {width: 6, height: 7};
    return r.getArea();
}
