// @exit 42
class Inner {
    protected int v;
    protected constructor(this.v = 0);
}

class Outer {
    protected Inner i;
    protected constructor(Inner i) {
        this.i = i;
    }
}

main() -> int {
    let a = new Inner();
    a.v = 42;
    let o = new Outer(a);
    return o.i.v;
}
