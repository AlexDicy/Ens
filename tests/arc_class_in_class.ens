// @exit 42
class Inner {
    int v;
    Inner(this.v = 0);
}

class Outer {
    Inner i;
    Outer(Inner i) {
        this.i = i;
    }
}

main() -> int {
    let a = new Inner();
    a.v = 42;
    let o = new Outer(a);
    return o.i.v;
}
