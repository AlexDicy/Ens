// @expect-error returns 'long', and the program entry point returns 'int' or nothing
// The entry point's result is the process exit code, so only 'int' or no return type is legal.
main() -> long {
    return 42L;
}
