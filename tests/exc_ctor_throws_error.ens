// @expect-error The constructor of 'Repo' cannot be marked 'throws'. A constructor never throws, so building an object never needs 'try'. Remove 'throws', and catch the exceptions with 'catch' clauses after the constructor body.
class Repo {
    constructor() throws { }
}
main() -> int { return 0; }