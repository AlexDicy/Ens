import Describable from left;
import Left from left;
import Box from left;
import left;

public class Right {
    public Left? partner;
    long value;

    public constructor(this.value);

    public tag() -> long {
        return this.value;
    }

    public partnerTag() -> long {
        if (this.partner != null) {
            return this.partner.tag();
        }
        return -1;
    }
}

public class Greeter implements Describable {
    public constructor();

    public override describe() -> string {
        return "greetings from right";
    }
}

public rightBase() -> long {
    return 20;
}

public rightValue() -> long {
    return left.leftBase() + 2;
}

public boxedRight() -> Box<Right> {
    return new Box<Right>(new Right(4));
}
